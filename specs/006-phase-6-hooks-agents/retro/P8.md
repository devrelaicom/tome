# Phase 6 / Phase 8 (Polish — Cross-Cutting Concerns + v0.6.0 cut) Retro

**Branch**: `006-polish`
**Baseline**: US5 merged @ de699d7 (PR #136).

## What this phase landed

<!-- Filled at closeout (T157). Planned scope: -->

- Phase-wide 4-reviewer pass (contract / Rust-lens / test / security) over the
  full Phase 6 surface (hooks + guardrails + agents + personas + the Phase 4
  rules-file correction) — catches cross-US drift the per-US passes can't.
- Phase-wide blockers + selected majors applied.
- Two new integration suites: `harness_sync_p6_idempotence.rs` (re-sync
  idempotence across all three sinks, mtime capture) and `entry_e2e_p6.rs`
  (enable → reconcile → assert hooks/guardrails/agents/persona end-to-end).
- cap-std / `O_NOFOLLOW` hardening evaluated across the four new file sinks
  (hooks settings, guardrails targets, Cursor sibling, agent files) per the
  Phase 5 P8 security backlog — applied or deferral documented.
- v0.6.0 cut: README + CHANGELOG + version bump; constitution check (PASS, no
  amendment — zero new top-level deps, zero new top-level modules).

## What Worked Well

<!-- Filled during the phase. -->

## What Didn't Work

<!-- Filled during the phase. -->

## Workarounds & Solutions

<!-- Filled during the phase. -->

## Packages & Dependencies

<!-- Filled during the phase. -->

## Patterns & Code

<!-- Filled during the phase. -->

## For Next Time

<!-- Filled during the phase. -->
