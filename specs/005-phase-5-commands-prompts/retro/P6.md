# Phase 5 / Phase 6 (US4) Retro — Middle-tier discovery + when_to_use indexing

**Slices**: US4.a (`get_skill_info` middle-tier tool), US4.b (`when_to_use` indexing + reindex re-eval), US4.c (`search_skills` truncation + reviewer pass + closeout)
**Phase 5 baseline**: PR #120 merged @ 37e3666. Test total 1139/143/16.

## What this phase will land

- New MCP tool: `get_skill_info` (middle-tier — between `search_skills`'s small ranked list and `get_skill`'s full body). Returns full description + when_to_use + plugin_version + user_invocable + path + 5-cap resource enumeration.
- `when_to_use` field contributes to embedding_text (US1.a already wired this; US4.b verifies + tests).
- `search_skills` extended with `description_max_chars` (default 150) + new `kind` field in result + `WHERE searchable = 1` filter.

## Workarounds

(filled as slices land)

## Patterns established

(filled as slices land)

## Decisions deferred

(filled as slices land)

## For next time

(filled at phase close)
