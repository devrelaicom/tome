# Phase 5 / Phase 7 (US5) Retro — Per-entry invocability flags + doctor extensions

**Slices**: US5.a (end-to-end matrix test), US5.b (plugin show + doctor extensions), US5.c (reviewer pass + closeout)
**Phase 6 baseline**: PR #123 merged @ 46433e2. Test total 1172/147/16.

## What this phase will land

- 4-entry matrix fixture (default-skill, default-command, disable-model-invocation, user-invocable + dormant)
- End-to-end test pinning the search/prompts surface filtering per flag combination
- `tome plugin show` per-entry annotations + Skills/Commands grouping per `contracts/catalog-and-plugin-extensions-p5.md`
- `tome plugin list` count format extended for commands
- `tome doctor` Phase 5 surface: prompts report, orphan data dirs, entry counts by kind, pending_re_embedding
- Read-only invariant: doctor must NOT lazy-create data dirs (FR-124)

## Last US before Polish

US5 is the final feature US. After this closes, only Polish + v0.5.0 release remain.

## Workarounds

(filled as slices land)

## Patterns established

(filled as slices land)

## Decisions deferred

(filled as slices land)

## For next time

(filled at phase close)
