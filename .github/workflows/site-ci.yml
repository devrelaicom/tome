name: Site CI
on:
  push:
    branches: [main]
    paths:
      - 'site/**'
      - '.github/workflows/site-ci.yml'
  pull_request:
    paths:
      - 'site/**'
      - '.github/workflows/site-ci.yml'

defaults:
  run:
    working-directory: site

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          # action-setup is a `uses:` step, so it ignores defaults.run.working-directory
          # and runs at the repo root (no package.json there). The pnpm version lives
          # in site/package.json "packageManager" — point it there explicitly.
          package_json_file: site/package.json
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm
          cache-dependency-path: site/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - name: Doc drift check
        run: pnpm drift:check
      - run: pnpm typecheck
      - run: pnpm build
      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium
      - name: E2E
        run: pnpm test:e2e
