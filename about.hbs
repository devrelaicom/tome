Third-party licence notices for the cargo dependency graph of `tome-mcp`.

This section is generated by `cargo about generate about.hbs` (FR-019 /
research §R-15) and lists every crate linked into the `tome` binary together
with its licence. The notices for the statically-linked native components that
live outside the cargo graph (ONNX Runtime, llama.cpp, vendored sqlite-vec)
are appended after this section in THIRD-PARTY-LICENSES.

Licence summary (by SPDX licence, crate count):

{{#each overview}}
  - {{{name}}}: {{count}} crate{{#unless (eq count 1)}}s{{/unless}}
{{/each}}

Full notices follow, one block per licence, listing the crates it covers.

{{#each licenses}}
--------------------------------------------------------------------------------
{{{name}}}
  Used by: {{#each used_by}}{{crate.name}} {{crate.version}}{{#unless @last}}, {{/unless}}{{/each}}
--------------------------------------------------------------------------------

{{{text}}}

{{/each}}
