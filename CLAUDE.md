# Tome — Claude Code Project Context

This file gives Claude Code persistent context about the Tome project. Keep it terse.

## Project

**Tome** is a Rust CLI (and eventually MCP server) that makes Claude Code's plugin ecosystem work across other agentic coding harnesses (Cursor, Codex, Gemini CLI, OpenCode, …).

- **Current phase:** **Phase 5 implementation in flight (v0.5.0-dev)** — planning complete (spec + plan + research + data-model + 9 contracts + quickstart + tasks.md). **US1 + US2 + US3 complete** across PRs #110–#119 + US3.d closeout (substitution layer is now fully operational end-to-end across all 4 stages); US4 (middle-tier discovery + when_to_use indexing) + US5 (per-entry invocability flags + doctor extensions) + Polish remain. Phase 4 (v0.4.0) shipped 2026-05-26 across PRs #103–#108. Phase 5 adds commands as first-class entries alongside skills (kind discriminator), exposes user-invocable entries as MCP prompts, ships a hand-rolled variable substitution layer (Tome built-ins + env passthrough + Claude Code-compatible argument substitution), adds a middle-tier `get_skill_info` MCP tool, and indexes `when_to_use` frontmatter for embedding. No new top-level dependencies (regex was already direct since Phase 1).
- **Phase 1 PRD (shipped):** [`PRDs/phase-1.md`](./PRDs/phase-1.md)
- **Phase 2 PRD (shipped):** [`PRDs/phase-2.md`](./PRDs/phase-2.md)
- **Phase 3 PRD (shipped):** [`PRDs/phase-3.md`](./PRDs/phase-3.md)
- **Phase 4 PRD (shipped):** [`PRDs/phase-4.md`](./PRDs/phase-4.md)
- **Phase 5 PRD (planning):** [`PRDs/phase-5.md`](./PRDs/phase-5.md)
- **Constitution:** [`CONSTITUTION.md`](./CONSTITUTION.md) (v1.3.0; Phase 5 introduces no constitutional amendments). Phase 3 introduced `tokio` inside `src/mcp/` only; Phase 4 introduced `llama-cpp-2` inside `src/summarise/` (sync); Phase 5 promotes `regex` from transitive to direct dep but introduces no new top-level crates.
- **Active spec:** [`specs/005-phase-5-commands-prompts/spec.md`](./specs/005-phase-5-commands-prompts/spec.md)
- **Active plan:** [`specs/005-phase-5-commands-prompts/plan.md`](./specs/005-phase-5-commands-prompts/plan.md)
- **Phase 5 design artefacts:** [`research.md`](./specs/005-phase-5-commands-prompts/research.md) (20 R-decisions), [`data-model.md`](./specs/005-phase-5-commands-prompts/data-model.md), [`contracts/`](./specs/005-phase-5-commands-prompts/contracts/) (9 contracts: exit-codes-p5, schema-migration-p5, frontmatter-p5, substitution-engine, mcp-tools-p5, mcp-prompts, entry-schema-p5, doctor-extensions-p5, catalog-and-plugin-extensions-p5), [`quickstart.md`](./specs/005-phase-5-commands-prompts/quickstart.md), [`tasks.md`](./specs/005-phase-5-commands-prompts/tasks.md) (281 tasks T001–T433).
- **Phase 4 spec (frozen):** [`specs/004-phase-4-refactor-harnesses/spec.md`](./specs/004-phase-4-refactor-harnesses/spec.md)
- **Phase 4 plan (frozen):** [`specs/004-phase-4-refactor-harnesses/plan.md`](./specs/004-phase-4-refactor-harnesses/plan.md)
- **Phase 4 design artefacts:** [`research.md`](./specs/004-phase-4-refactor-harnesses/research.md) (19 R-decisions), [`data-model.md`](./specs/004-phase-4-refactor-harnesses/data-model.md), [`contracts/`](./specs/004-phase-4-refactor-harnesses/contracts/) (13 contracts: paths-and-layout-p4, workspace-commands, harness-commands, sync-algorithm, harness-modules, rules-file-integration, mcp-config-integration, settings-composition, summariser, doctor-extensions-p4, catalog-and-plugin-extensions-p4, schema-migration-p4, exit-codes-p4), [`quickstart.md`](./specs/004-phase-4-refactor-harnesses/quickstart.md).
- **Phase 3 spec (frozen):** [`specs/003-phase-3-mcp-workspaces/spec.md`](./specs/003-phase-3-mcp-workspaces/spec.md)
- **Phase 3 plan (frozen):** [`specs/003-phase-3-mcp-workspaces/plan.md`](./specs/003-phase-3-mcp-workspaces/plan.md)
- **Phase 3 design artefacts:** [`research.md`](./specs/003-phase-3-mcp-workspaces/research.md) (15 R-decisions), [`data-model.md`](./specs/003-phase-3-mcp-workspaces/data-model.md), [`contracts/`](./specs/003-phase-3-mcp-workspaces/contracts/) (9 contracts: mcp-server, mcp-tools, workspace-resolution, workspace-init, workspace-info, doctor, schema-migration, catalog-extensions-p3, exit-codes-p3, log-format), [`quickstart.md`](./specs/003-phase-3-mcp-workspaces/quickstart.md).
- **Phase 2 spec (frozen):** [`specs/002-phase-2-plugins-index/spec.md`](./specs/002-phase-2-plugins-index/spec.md)
- **Codebase docs:** [`.sdd/codebase/`](./.sdd/codebase/) — all 8 documents refreshed 2026-05-25 against Phase 4 / US1-complete source via `/sdd:map incremental`.
- **Phase 4 / Foundational status:** F1–F11 shipped across PRs #59–#72. `Paths` collapsed under `<home>/.tome/`; central single SQLite DB; `WorkspaceName` newtype + `Scope(WorkspaceName)` tuple struct; `--global` flag deleted; `workspace_catalogs` + `workspace_skills` junctions are sole sources of truth for catalog enrolment and plugin enablement; plugin commands honour resolved workspace via SQL parameter (not hard-coded `WHERE name = 'global'`); catalog metadata derived from filesystem (no `config.toml` writes for catalogs); summariser + harness + settings module skeletons in place. See [`retro/P2.md`](./specs/004-phase-4-refactor-harnesses/retro/P2.md).
- **Phase 4 / US5 status:** `tome doctor [--fix] [--force] [--verify] [--json]` reports + repairs every Phase 4 subsystem end-to-end. **Subsystem enum** (`src/doctor/report.rs`) — 11 typed variants (`Embedder, Reranker, Index, Drift, Catalog(String), Schema, Summariser, Binding, BindingRulesCopy, HarnessRules(String), HarnessMcp(String)`) with custom `Serialize`/`Deserialize` preserving Phase 3 wire shape byte-for-byte ("embedder", "catalog:<name>", "harness-rules:<name>"). **`SubsystemHealth`** (`Ok, Drift, Broken, UserOwned, NotApplicable`) emitted per-harness when scope is GlobalFallback (US5.c-1 C-M1 fix). **Check helpers** in new modules: `doctor/binding.rs` (project-binding well-formedness + `RulesCopyState { Match, Missing, Drift, SourceMissing }` distinguishing absent-source vs absent-copy per US5.c-1 R-M5), `doctor/harness_integration.rs` (per-harness rules + MCP checks), `doctor/orphan_cleanup.rs` (5-layer defence-in-depth: STAGING_PREFIX + 1h mtime gate + symlink-skip + is_dir() + 0o700 staging perms). **`assemble_report`** never crashes (FR-561) — every check function collapses errors to `Broken` rather than `?`-bubbling (US5.c-1 C-M2 fix). **Repair classes** (FR-562) via `fixes::apply`: Summariser re-download, BindingRulesCopy re-copy (project-local via new `workspace::sync::sync_one_project` per US5.c-1 C-M3 fix; no longer broadcasts to sibling projects), HarnessRules + HarnessMcp re-sync (coalesced once per project per US5.c-1 R-M2 fix), Schema forward-migrate, orphan `.tome.tmp.*` cleanup. **NOT-auto-fixable**: Binding broken when marker names missing workspace (developer chooses rebind vs recreate); HarnessMcp UserOwned without `--force` (override path documented). **`--force` flag**: rewrites in-play UserOwned HarnessMcp entries ONLY (US5.c-1 S-M2 precise scoping); doesn't bypass Binding broken. **`--force` without `--fix`** → exit 2 Usage error (US5.c-1 R-M1 fix). **Exit codes**: 0 healthy / 1 still-degraded / 75 (`DoctorFixNotSafe`) unfixable / 2 Usage. **MCP `search_skills` query length cap** (US5.a T373) — 4096 chars at input boundary; structured `query_too_long` envelope. 4-reviewer pass surfaced **1 blocker + 21 majors**; PR #101 applied 1 blocker + 10 majors. Test total 862 → 916 across 117 → 125 suites; ignored 16. New dev-dep `filetime = "0.2"` for mtime backdating in orphan tests. See `retro/P7.md`.
- **Phase 4 / US4 status:** Bundled local-LLM summarisation ships end-to-end. Production `LlamaSummariser` (`src/summarise/llama.rs`) loads Qwen2.5-0.5B-Instruct via `llama-cpp-2 = "=0.1.146"`; SHA-256 verified against the registry-pinned digest `74a4da8c9fdbcd15bd1f6d01d621410d31c6fc00986f5eb687824e7b93d7a9db` (491,400,032 bytes; US4.d-1 C-B1 fix — F6 shipped placeholder, US4.a's brief didn't flip it, 4-reviewer pass caught it). Model is loaded ONCE at `LlamaSummariser::new()` and cached on self (US4.d-1 S-M4); per-trigger `summarise()` calls reuse the model and create a fresh `LlamaContext` per pass (llama-cpp-2 has no KV-cache reset hook). `LlamaBackend` is a process-singleton via `OnceLock<Result<LlamaBackend, _>>`; `summarise::backend()` uses `PoisonError::into_inner` recovery (US4.d-1 R-M7). Inference params: temperature 0.3 / top_p 0.9 / repeat_penalty 1.1; outputs decoded via `encoding_rs::Decoder` (direct dep added because llama-cpp-2's `token_to_piece` requires it; not re-exported). Length-window enforcement: `SHORT_MAX_CHARS = 800` and `LONG_MAX_CHARS = 2500` (single source of truth in `src/summarise/mod.rs` per US4.d-1 C-B3/R-B1 fix); over-limit emits `tracing::warn!` but keeps the value (FR-425); empty → `SummariserFailure { kind: OutputEmpty }` exit 24; non-UTF8 → `OutputUnparsable` exit 24. Triggered regeneration wired to 5 sites (FR-380/381/382/365 + US4.d-1 R-M6 cascade): `plugin enable`, `plugin disable`, `reindex` (gated on hash change via `ReindexAggregate::any_changes()`), `catalog update` (per-workspace), `catalog remove --force` (per affected workspace). FR-385 forward-progress: skill mutation tx commits FIRST; summariser invoked OUTSIDE the tx; failure exits 24 with mutation retained; doctor (US5) detects stale state. Pragmatic carve-out: `ModelMissing` is a silent no-op in trigger callers (on-demand model, FR-420; documented in `contracts/summariser.md` per US4.d-1 C-M2); explicit `tome workspace regen-summary` STILL hard-fails. MCP server reads cached short summary at startup via `mcp/tool_description.rs::compose(scaffold, cached_short)` + `warn_if_too_long(desc)`; description visible to every connected client; in-memory until server restart (NFR-103: MCP doesn't invoke the summariser in-process). Test infrastructure: `SummariserOverrideGuard` RAII helper for test-injection (`SUMMARISER_OVERRIDE` process-global slot; same pattern as `HARNESS_MODULES_OVERRIDE` / `MIGRATIONS_OVERRIDE`); bespoke `tracing_subscriber` capture layer for warn-event assertions (US4.d-1 T-M5; avoids `tracing_test` dep + `callsite::interest` cache hazard). 4-reviewer pass surfaced 5 blockers + 21 majors; PR #97 applied all 4 actionable blockers + 9 majors. Test total 825 → 862 across 110 → 117 suites; ignored 16. New deps: `llama-cpp-2 = "=0.1.146"`, `encoding_rs = "0.8"`. See `retro/P6.md`.
- **Phase 4 / US3 status:** Layered settings + composition resolver ships end-to-end. The F8 skeleton `resolve_effective_list` is now hardened (DFS cycle path renders walk-order chain per FR-445; priority walk stops at first declarer per FR-441; references resolve to directly-declared lists per FR-449; per-entry unsupported-name validation per FR-460) and exercised in production via `CentralDbScopeProvider` (US3.d-1 fix: replaces `StubScope::new()` placeholder in `harness::sync::sync_project` + all `tome harness *` commands). Provider distinguishes "workspace exists with settings.toml" (Ok(Some)) / "workspace exists, no settings.toml" (Ok(None) — legal) / "unknown workspace" (exit 13) / "IO or parse error" (exit 70). Composition validation rules: `[workspace]` outside project → exit 17; `[workspace]` in project with no bound workspace → exit 17; `[workspaces.<name>]` to non-existent workspace → exit 13; `!`-prefixed bracketed forms (`![global]`, `![workspaces.foo]`) → exit 17 BadExclusion; harness name not in SUPPORTED_HARNESSES → exit 18 (per-entry, not deferred to end-of-resolution). Full `tome harness` command surface: bare tabular (5 modules in lex order), `list [<workspace>]` (effective with source_chain annotations + excluded section, OR as-written verbatim with DB-membership check), `use <name> --scope project|workspace|global [--force]` (toml_edit surgical edit under `index.lock`; runs sync if effective list changed; informational notice if unchanged), `remove <name> --scope ...` (mirror semantics; preserves empty `harnesses = []`), `info <name>` (with composition references via `Vec<HarnessReference { scope, via: Option<String> }>`), `sync` (thin wrapper over `harness::sync::sync_project`, byte-for-byte idempotent). New `settings::edit` module wraps `toml_edit::DocumentMut` for surgical edits with comment + order preservation; routes through hardened `catalog::store::write_atomic`. Test infrastructure: `HOME_MUTEX` + `HomeGuard` RAII helper serialises process-global HOME mutations across parallel tests; `(HarnessModulesGuard, MutexGuard)` tuple-drop discipline (PR #90) prevents race where outgoing test's guard clears incoming test's override. 4-reviewer pass surfaced 6 blockers + 29 majors; PR #92 applied all 6 blockers + 12 selected majors. Test total 677 → 825 across 92 → 110 suites; ignored 17. No new dependencies; `toml_edit` now consumed by 4 sites. See `retro/P5.md`.
- **Phase 4 / US2 status:** Full workspace-lifecycle command surface ships: `tome workspace init <name> [--inherit-global]`, `list`, `info [<name>]`, `rename <old> <new>`, `regen-summary <name>` (required positional per contract), `remove <name> [--force]`, `sync [<name>]`. Storage: workspace rows in `workspaces` table; per-workspace state at `<root>/workspaces/<name>/{settings.toml, RULES.md}`. `init` lands the directory atomically via `util::atomic_dir::land_directory` under the advisory lock, with `--inherit-global` copying `workspace_catalogs` rows from global into the new workspace in the same DB transaction. `rename` rewrites bound-project markers via `toml_edit::DocumentMut` (preserves the optional `harnesses` field per data-model §7) THEN opens the SQL transaction for the workspace name UPDATE; partial-failure mode (markers rewritten, SQL UPDATE failed) surfaces via doctor `Binding` subsystem in US5. `remove`'s 5-step cascade per FR-405: (1) per-project integration teardown via narrowed effective list (`settings::resolver::resolve_effective_list` with `StubScope::new()`), (2) per-project marker dir removal, (3) single DB tx for skill/catalog/project/workspace row deletion, (4) central workspace dir removal, (5) refcount-clean catalog caches no longer referenced. `regen-summary` is the first production consumer of the `Summariser` trait (`LlamaSummariser` still BackendInitFailed until US4.a; `StubSummariser` exercised in tests); writes `[summaries]` table via `toml_edit::DocumentMut` (preserves `[[catalogs]]`/`harnesses` + comments), rewrites `RULES.md` from the long summary, calls `workspace::sync_one` to propagate, bumps `last_used_at` per FR-411. `sync` exposes the public CLI wrapping the helper introduced in US2.a-2 with per-workspace + all-workspaces iteration. `catalog::store::write_atomic` hardened with mode preservation + symlink refusal (US2.d-1 S2-M1/M2; lifted from `harness::rules_file::atomic_write` to become the single source-of-truth atomic-write outside `src/harness/`). 4-reviewer pass surfaced 4 blockers + 23 majors; all 4 blockers + 11 majors applied in PR #86, remainder deferred per [`review/us2-disposition.md`](./specs/004-phase-4-refactor-harnesses/review/us2-disposition.md). New test surface: `workspace_{init,list,rename,regen_summary,remove,remove_cascade,sync}.rs` + 4 byte-stable JSON wire-shape pins. Test total 609 → 677 across 82 → 92 suites; ignored 29 → 17. No new dependencies; no new error variants beyond the F3 pre-allocations. See [`retro/P4.md`](./specs/004-phase-4-refactor-harnesses/retro/P4.md).
- **Phase 4 / US1 status:** `tome workspace use <name> [--force] [--json]` ships end-to-end against the real claude-code harness module. Bind algorithm runs Phase A (advisory lock + workspace_projects UPSERT + workspaces.last_used_at update in a single transaction + `<project>/.tome/` atomic-populated-directory landing via `util::atomic_dir::land_directory_with_replace`) then Phase B (harness sync without holding the lock — per `contracts/sync-algorithm.md`). Sync orchestrator (`harness::sync::sync_project`) computes the effective harness list via `settings::resolver::resolve_effective_list` (project marker → workspace settings → global settings priority walk; `StubScope` until US3.a wires the DB-backed `ScopeProvider`), dispatches per-harness writes through the `HarnessModule` trait, runs the cleanup pass for harnesses not in the effective list, dedupes multi-harness shared rules-file and MCP-config paths. `HarnessClash` → exit 19 with FR-403 forward-progress (binding remains committed even on sync failure). Mode preservation on atomic rewrites (S-M3): existing file's mode is captured via `symlink_metadata` and chmod'd onto the staged tempfile before `NamedTempFile::persist` — prevents silent 0644 → 0600. Symlink refusal at every read/write entry point. Non-UTF8 project paths refused up front (R-B1) to avoid lossy round-trips into the `workspace_projects` PK. New module surface: `src/util/atomic_dir.rs`, `src/workspace/binding.rs`, `src/commands/workspace/use_.rs`, `src/commands/harness/mod.rs`, `src/harness/{sync,rules_file,mcp_config,stub}.rs`, `src/harness/{claude_code,codex,cursor,gemini,opencode}.rs`. Test surface: 19 new test files (`workspace_use_*`, `harness_*`, `rules_file_*`, `mcp_config_*`, `sync_*`, `harness_module_claude_code`). Test count 490 → 609 across 64 → 82 suites. Reviewer pass (4 agents) produced 3 blockers + 25 majors; 12 applied in PR #80, remainder deferred per [`review/us1-disposition.md`](./specs/004-phase-4-refactor-harnesses/review/us1-disposition.md). Contract amendment in `mcp-config-integration.md` § Ownership marker clarifies that `env` preservation applies only to Tome-owned rewrites. See [`retro/P3.md`](./specs/004-phase-4-refactor-harnesses/retro/P3.md).
- **US1 status:** `tome plugin enable | list | show` + `tome query` ship end-to-end. Manual SC-001 / SC-002 against real BGE models still pending — see `retro/P3.md` § "T088 manual verification".
- **Phase 3 / US2 status:** `tome workspace info` + `tome workspace init` ship end-to-end. Info is a narrow read-only report — never takes the advisory lock, bootstrap-not-yet is informational not an error. Init atomically lands `.tome/` via `tempfile::Builder::tempdir_in` + `TempDir::keep()` + `std::fs::rename` so a SIGINT mid-init never produces a partial. `--inherit-global` seeds the new workspace's `[catalogs]` from the global config (enablement is intentionally NOT copied — it lives in the index DB). `--force` renames existing `.tome/` aside to `.tome.old/` first; rollback on staging-rename failure. Opt-in workspace registry at `${state_dir}/workspaces.txt` is appended-to only when the file already exists. No new error variants — reuses `CatalogAlreadyExists` (4), `Io` (7), `WorkspaceMalformed` (70), `IndexIntegrityCheckFailure` (51). See `retro/P4.md`.
- **Phase 3 / US3 status:** every Phase 1/2 command honours the resolved `Scope` end-to-end (the per-command refactor itself landed in Foundational F1; US3.a is the cross-product proof in `tests/workspace_commands.rs`). On-disk catalog clones at `paths.cache_dir_for(url)` are now reference-counted: `catalog::store::reference_count(url, paths) -> Vec<Scope>` walks the global config + every workspace in the opt-in `workspaces.txt`. `catalog add` reuses an existing clone if any scope already has the URL (no re-clone, manifest re-parsed from cache). `catalog remove` only `remove_dir_all`s the clone when no scope still references it. TOCTOU is unlocked and named — same profile as Phase 2's `cascade_disable_for_catalog` pre-check. Without the `workspaces.txt` opt-in, `reference_count` is global-only; documented degradation. No new error variants. See `retro/P5.md`.
- **Phase 3 / US5 status:** forward-schema-migration framework end-to-end tests ship — `tests/schema_migration_e2e.rs` (5 tests) exercises `apply_pending` against synthetic v0/v0→v2/mid-failure/v99 fixtures via `MIGRATIONS_OVERRIDE` injection. Test-only changes: no production code modifications (the framework itself shipped in Foundational F7). Phase 3 still ships `pub const MIGRATIONS: &[Migration] = &[]` — zero registered migrations by design; the first real migration lands in Phase 4+. `tests/atomicity.rs` extended with `migration_abort_mid_transaction_…` (closure-level `Err`, not `CANCELLED` static, to avoid racing other tests in the binary). `tests/doctor.rs` extended with `fix_runs_forward_schema_migration_end_to_end` — bootstraps a real v1 DB with the production registry seeds, downgrade-stamps to v0, injects a synthetic `subsystem: "schema"` `SuggestedFix` (Phase 3's `build_suggested_fixes` doesn't emit one), and verifies the marker table created inside the migration is present after `doctor::fixes::apply` + `re_assemble`. New helper `tests/common/mod.rs::write_index_db_with_schema_version(path, version)` fabricates a minimal `meta`-only synthetic DB; no committed binary `.db` files. No new dependencies. No new error variants. See `retro/P7.md`.
- **Phase 3 / US4 status:** `tome doctor [--fix] [--verify]` ships as the broad diagnostic. Library API `doctor::assemble_report(scope, paths, home, verify)` builds a `DoctorReport` with workspace + models + index + drift + catalog cache state (per-catalog: Ok/Missing/NotARepo/ManifestInvalid) + harness presence (six well-known dirs at `~/.{claude,codex,cursor,gemini,opencode,continue}/`, existence-only probe per FR-167) + suggested fixes + overall classification. Three auto-fix repair classes via `doctor::fixes::apply`: model re-download (`embedding::download::download_model`), catalog re-clone (`Git::clone_shallow` against recorded URL+ref), schema forward-migrate (under the resolved scope's advisory lock). `--fix` re-runs each affected check in place; `re_assemble` rebuilds suggested_fixes + overall without re-probing catalogs/harnesses. Exit codes: 0 (Ok), 1 (Degraded/Unhealthy), 75 (`--fix` ran but un-fixable issues remain — manifest-invalid, drift, schema-too-new). Status's `check_model` / `check_index` / `check_drift` promoted from private to `pub` so doctor reuses them — single source of truth across the two diagnostic surfaces. See `retro/P6.md`.
- **US2 status:** Bare `tome plugin` drops into an interactive catalog → plugin → action flow (`run_interactive`) with Back/Quit at every level; non-TTY exits 54 (`NotATerminal`) per FR-051. Tested via `rexpect` pty harness.
- **US3 status:** `tome plugin disable <id>` ships with `--force` short-circuit; non-TTY without `--force` exits 54 (`NotATerminal`) with a documented pointer message. Cheap re-enable (FR-006) verified — `enable_plugin_atomic` skips the embedder when `content_hash` matches.
- **US4 status:** `tome models download | list | remove` ships against the existing `embedding::download` library + pinned `MODEL_REGISTRY`. `--force` on download + remove, `--verify` on list (rehashes via `embedding::download::sha256_file`). Non-TTY without `--force` on remove → exit 54 with pointer message. CLI download path NOT exercised in CI (real network fetch) — coverage is library-level via `tests/model_download.rs` + CLI skip-path via sparse-file fabricators.
- **US5 status:** `tome catalog update` now reindexes every enabled plugin in each refreshed catalog (cheap-skip unchanged, re-embed modified, drop removed). Plugins gone upstream auto-disable per FR-033. `tome reindex [<scope>] [--force]` ships as the explicit recovery path — scope grammar is omitted | `<catalog>` | `<catalog>/<plugin>`. Per-plugin atomicity (each `lifecycle::reindex_plugin` acquires its own advisory lock).
- **US6 status:** `tome status [--verify] [--json]` ships as the read-only doctor / pre-flight. Reports embedder + reranker on-disk state, index integrity, plugin/skill counts, drift detection. Exit 0 / 1 / 1 for Ok / Degraded / Unhealthy. Never takes the advisory lock — works even when a writer is running. Extended `tome --version` includes embedder + reranker identities; `--version --json` emits the structured form. Pre-parse hook in `main.rs` (clap's `disable_version_flag = true`).
- **US7 status:** `tome catalog remove <name>` refuses with exit 53 (`CatalogHasEnabledPlugins`) when the catalog has enabled plugins and `--force` is not passed. `--force` cascades disable + row drop for each enabled plugin inside ONE advisory-lock window (single-lock-per-batch, distinct from the per-plugin pattern in catalog update / reindex), then proceeds with Phase 1 catalog removal. New library helper `lifecycle::cascade_disable_for_catalog`. JSON `removed` envelope gains an optional `cascade` array. 240 tests pass across 36 suites.

## Active Technologies

### Phase 1 (shipped, unchanged)

- **Language**: Rust stable, MSRV pinned at `rust-version = "1.93"` (verified in CI).
- **CLI**: `clap` (derive feature) — `--help` / `--version` / global flags.
- **Config / manifest**: `serde` + `serde_derive`, `toml` — Tome-owned structs use `#[serde(deny_unknown_fields)]`.
- **Errors**: `thiserror` for the closed `TomeError` enum (drives exit codes); `anyhow` for application-level context chaining.
- **Logging**: `tracing` + `tracing-subscriber` (stderr only; orthogonal to `--json`).
- **Paths**: `directories` (XDG-aware).
- **Hashing**: `sha2`, `hex` (cache directory naming, model checksums, content hashes).
- **Atomic writes**: `tempfile` (registry, catalog cache, models dir atomicity).
- **Signal handling**: `ctrlc` (SIGINT cancellation; exits with code 8).
- **Colour / NO_COLOR**: `anstream` + `anstyle` (transitive via clap 4).
- **Regex**: `regex` (credential scrubbing in `src/catalog/git.rs`).
- **Time**: `time` (RFC 3339 timestamps).
- **Semver**: `semver`.

### Phase 2 additions

- **Embedded database**: `rusqlite` with the `bundled` feature — statically linked SQLite, no system dep.
- **Vector search**: `sqlite-vec` C extension vendored under `vendor/sqlite-vec/`, compiled in via `build.rs`.
- **Inference**: `fastembed-rs` wrapping `ort` (ONNX Runtime). CPU execution provider only; CUDA / CoreML / DirectML disabled.
- **Models** (downloaded at runtime; not bundled): `bge-small-en-v1.5` INT8 (~45 MB, MIT), `bge-reranker-base` INT8 (~280 MB, MIT). Stored under `${XDG_DATA_HOME}/tome/models/`.
- **Progress / spinners**: `indicatif`.
- **Tables**: `comfy-table`.
- **Colours**: `owo-colors` (native `NO_COLOR`).
- **Prompts**: `inquire` (Select / MultiSelect / Confirm; refuses on non-TTY).
- **HTTP**: `reqwest` with `blocking` + `rustls-tls`, `default-features = false`.

**Strictness boundary** (FR-013a): `#[serde(deny_unknown_fields)]` applies to Tome-owned inputs (config, model `manifest.json`, index `meta` rows). Third-party inputs (`plugin.json`, `SKILL.md` YAML frontmatter) parse leniently — forward-compat with upstream additions.

**Not used**: `tokio`, `libgit2`/`git2`, `atty`, `colored`, `lazy_static`, `once_cell` (std `OnceLock` covers it). Phase 2 stays synchronous.

## Architectural Constraints (from the constitution)

- **Sync only.** No async runtime. `reqwest::blocking`, `rusqlite`, and `fastembed-rs` are all sync. The MCP server is the future forcing function for async.
- **Inherit `git`.** Shell out to system `git`. Never vendor a Git library.
- **Closed error set.** `TomeError` has no `Other`/`Unknown` arm. Every Phase 2 failure class has its own enumerated variant and exit code (see `specs/002-phase-2-plugins-index/contracts/exit-codes.md`).
- **Strictness boundary.** Tome-owned declarative inputs are strict (`#[serde(deny_unknown_fields)]`); third-party inputs (`plugin.json`, SKILL.md frontmatter) are lenient — see spec FR-013a. The strict-on-Tome-owned principle is enforced by `tests/manifest_strictness.rs`.
- **Atomic writes.** Registry, cache, models directory, and index DB mutations are atomic. SQLite WAL + a Tome-owned advisory lockfile (`index.lock`) provide the index concurrency contract (FR-040).
- **Credential scrubbing at the boundary.** `git::scrub_credentials` extends to model download URLs and `reqwest` error chains.
- **50 MB binary cap** (revised from 10 MB on 2026-05-13 — see `retro/P3.md` §"Binary-size cap revision"). CI asserts `stat -c%s target/release/tome` on Linux. `ort` (CPU-only static) is the load-bearing dep; profile is `lto = "thin"`, `panic = "abort"`, `strip = "symbols"`. If breached, the plan revises — the discipline is non-waivable (NFR-001); the specific number is sized to current reality plus headroom.
- **Licence allowlist.** Unchanged. Every Phase 2 dep verified inside the allowlist. `cargo-deny` enforces. Downloaded models (BGE family, MIT) are surfaced in `tome models list`.

## Conventions

- **Commits**: Conventional Commits. Enforced locally by `cocogitto` (`cog verify`) in the `commit-msg` hook (versioned under `.githooks/`). Format: `type(scope): subject`.
- **Branching**: trunk-based; short-lived branches off `main`.
- **PRs**: small batches — ~400 lines or 2 modules max as a soft cap.
- **Comments**: explain *why*, not *what*. Reader knows Rust.
- **Modules**: capability-organised. Phase 1: `catalog`, `config`, `paths`, `error`, `output`, `logging`. Phase 2 adds: `plugin` (manifest/frontmatter/lifecycle), `index` (db/schema/migrations/vec-ext/skills/query/meta/integrity/lock), `embedding` (fastembed wrapper + stub + registry + download + runtime), `presentation` (wraps comfy-table / indicatif / owo-colors / inquire). Phase 3 adds: `workspace` (scope / resolution / inventory) and `mcp` (mod / runtime / log / preflight / server / state / tools — the one async island, structurally enforced by `tests/sync_boundary.rs`).
- **Errors**: `thiserror` inside modules; `anyhow` at the application boundary.
- **Field-method coexistence** (Phase 3 / F1): When a refactor would rename a public struct field across many call sites, add accessor methods first (e.g. `Paths::config_file_for(&Scope)` alongside the existing `Paths.config_file` field). The accessor's `_for(&Scope)` suffix often communicates intent better than the renamed field would have; the rename can then be deferred or shelved.
- **Specific-over-generic exit codes** (Phase 3 / F8 preflight): When a failure matches an existing Phase 1/2 variant, emit that code rather than a generic residual (`ModelMissing` 30 / `ModelChecksumMismatch` 32 / `EmbedderNameDrift` 41 / `SchemaVersionTooNew` 73 all win over `McpStartupFailed` 60). The closed `TomeError` set keeps every alternative discoverable.
- **Test injection via `#[doc(hidden)] pub static`** (Phase 3 / F7): Integration tests under `tests/` consume the library without `cfg(test)` visibility, so `#[cfg(test)] thread_local!` injection points are unreachable from there. Gate with `#[doc(hidden)] pub static` instead; document the test-only intent in the doc comment. Phase 7's `tests/schema_migration_e2e.rs` is the first consumer.
- **Silent compute / emit wrapper pattern** (Phase 3 / US1.b): When a CLI command's compute path is reused by a non-CLI surface (MCP, library API, RPC), split into a `pipeline(args, deps) -> Result<Outcome, Error>` (no I/O side-effects) plus a `run_with_deps(args, deps, mode) -> Result<Outcome, Error>` that calls `pipeline` then emits per `mode`. Tests + alternative callers use `pipeline`; the CLI uses `run_with_deps`. First applied splitting `query::run_with_deps` so `mcp::tools::search_skills` could reuse the KNN+rerank logic without the stdout/stderr emit step (FR-221 — protocol channel is sacred).
- **`spawn_blocking` for sync work inside async handlers** (Phase 3 / US1): MCP tool handlers are `async fn` (required by rmcp) but their actual work (rusqlite queries, fastembed inference, fs::read) is sync. Pattern: do cheap async-friendly work directly (validation, config load), then `tokio::task::spawn_blocking(move || { ... sync work ... }).await`. The single-threaded runtime stays responsive; inference latency doesn't hold up the reactor.
- **Atomic populated-directory landing** (Phase 3 / US2.b): when a multi-file directory must appear either complete or not at all to readers, build it in a sibling staging dir on the same filesystem and rename once. Pattern: `tempfile::Builder::new().prefix(".tome.tmp.").tempdir_in(target_parent)?` → populate → `staging.keep()` (drops the auto-cleanup guard, returns `PathBuf`) → `std::fs::rename(staged, target)`. `std::fs::rename` is POSIX-atomic when source and target share a filesystem; the staging-inside-target-parent invariant makes that true. Distinct from `NamedTempFile::persist` (file-level). For replace-existing semantics, rename the existing target aside to `.<name>.old/` first, then rollback on staging-rename failure. Used in `src/workspace/init.rs`; `tempfile::TempDir::persist` doesn't exist (the contract referenced it; we use `keep()` + `std::fs::rename` instead).
- **Opt-in registry file** (Phase 3 / US2.b): for "track this for me, but only if I ask" features, write a helper like `inventory::append_if_registry_exists(path, item)` that no-ops when the registry file is absent. The user touches the file once to opt in; subsequent operations append. Dedupe by exact-string match against the canonicalised item. Pattern lives in `src/workspace/inventory.rs`; reusable for any future opt-in tracking surface.
- **Emit-only `Serialize` records** (Phase 3 / US2): `WorkspaceInfo`, `InitOutcome`, and similar wire-shape types are never deserialised — they exist to render `--json` for stdout. No `#[serde(deny_unknown_fields)]` because the strictness boundary only applies to inputs. Pin the wire format with a byte-stable JSON serialisation test instead (e.g. `tests/workspace_info.rs::info_json_shape_is_byte_stable_for_bootstrap_not_yet`).
- **Reuse existing closed-set variants over promoting new ones** (Phase 3 / US2.b): when a new failure mode maps semantically onto an existing `TomeError` variant + exit code, prefer the reuse over a dedicated variant. Cost: a slightly off Display message (e.g. `CatalogAlreadyExists("workspace at /path/.tome")` displays as "catalog `workspace at /path/.tome` is already registered"). Benefit: zero variants added, zero exit-code-table churn, zero JSON envelope shape change. Promote to a dedicated variant only if a specific failure mode emerges that needs to be distinguished from the existing one. The contract explicitly green-lit this reuse for workspace-already-initialised → `CatalogAlreadyExists` (4).
- **Content-addressed shared resource with reference counting** (Phase 3 / US3.b): when an on-disk resource is shared across scopes (e.g. the `catalogs/<sha256>/` cache directories), don't unconditionally delete on remove. Build an ad-hoc reference table by enumerating every scope's config that could reference the URL/key, and only delete when the count reaches zero. Implementation lives in `catalog::store::reference_count(url, paths) -> Vec<Scope>`. The reference enumeration is unlocked — concurrent removes race benignly (one wins, the other no-ops); a remove + add race may leave a dangling reference recoverable by re-fetching. Same TOCTOU profile as Phase 2's `cascade_disable_for_catalog` pre-check. Reusable for any future shared-on-disk resource.
- **`reuse_existing: bool` rollback gating** (Phase 3 / US3.b): when a function may either CREATE a resource or REUSE an existing one (e.g. `catalog::add` — clone or refcount-reuse), name the boolean after the path taken ("did we take the reuse path?") rather than after the rollback action ("should we delete on save failure?"). The save-failure rollback then becomes `if !reuse_existing { remove_dir_all(...) }` — read-time intent matches the question being asked.
- **F1's mechanical sweep paid off in US3** (Phase 3 / US3.a): when a Foundational slice does a wholesale mechanical sweep (in this case threading `ResolvedScope` and `Paths::*_for(&scope)` through every command), the downstream user-story slices that "use" the sweep often reduce to "prove it works end-to-end" — i.e. the integration test IS the deliverable, with no production code changes. Cost: zero. Benefit: the verification commit doubles as the regression net.
- **Helper-visibility promotion to share state across surfaces** (Phase 3 / US4): when two surfaces must report the same value for the same subsystem (e.g. `tome status` + `tome doctor` on models / index / drift), promote the shared helper from private to `pub` rather than duplicating compute. Three-character edit, zero risk of the two surfaces disagreeing. Documented as "read-only diagnostic helpers" so future callers know the intent. Applied to `commands::status::check_model` / `check_index` / `check_drift`.
- **`home: &Path` as test-isolation hook** (Phase 3 / US4): when a library function would otherwise read `$HOME` directly, accept it as a parameter and have the CLI wrapper resolve env. Tests pass a `TempDir`-rooted path; production passes the real `$HOME`. Drops the dependency on `serial_test` for env-isolated tests. Applied to `doctor::assemble_report(scope, paths, home, verify)` for harness-probe testability; same idiom applies to any future read-from-env operation.
- **`subsystem` string routing for tagged repair lists** (Phase 3 / US4): when a list of items needs per-item dispatch (here: `SuggestedFix.subsystem` values like `"embedder"` / `"reranker"` / `"catalog:<name>"` / `"schema"`), encode the dispatch key in a string field rather than promoting to an enum. `.strip_prefix("catalog:")` extracts per-instance data. Flat if-else dispatch ladder, wire-friendly. Trade-off: less type safety than an enum; acceptable up to ~6 arms.
- **`re_assemble` post-mutation pattern** (Phase 3 / US4): when an "apply" function mutates per-field state in place (e.g. `fixes::apply_one` updates `report.embedder` / `report.catalogs` after each repair), expose a sibling `re_assemble` that recomputes derived state (`suggested_fixes`, `overall`) from those fields without re-running expensive checks. Halves the FS cost on the catalog-enumeration + harness-probe sides.
- **RAII `MigrationsGuard` for thread-local injection** (Phase 3 / US5): `MIGRATIONS_OVERRIDE` is a `thread_local!` per F7 convention. Tests install it via a 1-line `MigrationsGuard::install(&[...])` whose `Drop` impl clears the slot — survives panics, no manual teardown, no cross-test leakage. The migration step itself is a named `fn` item (not a closure) because `Migration.apply` is `fn(&Transaction) -> Result<(), TomeError>`. Reusable shape for any future test-only `thread_local!` injection point: pair the `pub static` with a guard struct in the consumer test file. Used in `tests/schema_migration_e2e.rs`, `tests/atomicity.rs`, `tests/doctor.rs`.
- **Generate-at-setup fixture DBs over committed binaries** (Phase 3 / US5): when a test needs an "older-schema" or "newer-schema" SQLite file, write a helper like `write_index_db_with_schema_version(path, version)` that bootstraps a minimal `meta`-only DB at the requested version. Beats committing `tests/fixtures/older-schema.db` binaries: no binary churn in PR diffs, no opaque blobs in git history, no surprise when the production schema layout drifts. The fixture intentionally has *only* `meta` so synthetic migrations can create whatever tables they want without conflicting with the production schema.
- **SIGINT mid-tx modelled as deliberate `Err`** (Phase 3 / US5): the `catalog::git::CANCELLED` static cannot be used inside test bodies because cargo runs tests in the same process — flipping it races every other test in the binary (same discipline as `atomicity_enable.rs`). For "interrupted mid-transaction" tests, return a deliberate `Err` from the migration closure: the per-step transaction rollback is identical regardless of whether the abort came from a signal handler or a closure-level failure, so the assertion targets (table absent, schema_version unchanged) are the same. Generalisable to any test that wants to verify rollback behaviour without process-global signal manipulation.
- **Synthetic SuggestedFix injection for dispatch-path testing** (Phase 3 / US5): when a framework SHIPS the dispatch path for a class of fixes but DOESN'T YET produce them (Phase 3 ships zero registered migrations, so `build_suggested_fixes` never emits `subsystem: "schema"`), tests can manually push a synthetic `SuggestedFix { auto_fixable: true, ... }` into the assembled report before calling `doctor::fixes::apply`. The dispatch happens through the real production code; only the *trigger* is synthetic. Reusable shape for any framework whose production-side trigger logic ships separately from its dispatch + execute logic. Applied in `tests/doctor.rs::fix_runs_forward_schema_migration_end_to_end`.
- **Framework ships empty, e2e tests live on synthetic fixtures** (Phase 3 / US5): the schema-migration framework ships with `pub const MIGRATIONS: &[Migration] = &[]` — zero registered migrations. The Phase 3 e2e tests don't add real migrations; they exercise the framework via `MIGRATIONS_OVERRIDE` injection so Phase 4+ migrations land on tested rails. Pattern: when shipping a framework whose first production consumer is a future phase, ship the test harness (override hook + e2e tests against synthetic registrations) NOW so the rails are battle-tested before any real migration runs against a user's DB.

## Common Commands

```sh
# Build / run
cargo build                                      # debug build
cargo build --release                            # release build (used by CI binary-size check)
cargo run -- catalog list                        # run a subcommand from source

# Quality gates (also enforced by the .githooks/pre-commit hook)
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
typos

# Tests (.githooks/pre-push runs the full suite)
cargo test                                       # all tests (uses stub embedder — fast, no model files)
cargo test --test catalog_add                    # one integration test file
cargo test catalog_add::                         # one test by path
cargo test --test query                          # Phase 2 query tests
cargo test --test concurrency                    # two-process index contention
cargo test --test atomicity                      # interrupt-injection tests

# Security and dependency hygiene
cargo audit
cargo deny check

# Conventional Commits
cog verify --file <commit-msg-file>

# Git hooks (versioned under .githooks/; no external manager)
git config core.hooksPath .githooks              # one-time, per clone
.githooks/pre-commit                              # run the pre-commit chain manually
.githooks/pre-push < /dev/null                    # run pre-push manually (drain empty stdin)

# MSRV verification (CI uses dtolnay/rust-toolchain @ rust-version from Cargo.toml)
cargo +<MSRV> build
```

## File Structure

```
src/
├── main.rs                   # entry: parse → dispatch → map errors → exit
├── lib.rs                    # re-exports
├── cli.rs                    # clap derive defs + global flags
├── error.rs                  # closed TomeError enum + ExitCode mapping
├── config.rs                 # config.toml (strict)
├── paths.rs                  # XDG paths (Phase 1) + index_db, models_dir, index_lock (Phase 2)
├── output.rs                 # human/--json formatter, NO_COLOR, TTY detection
├── logging.rs                # tracing-subscriber wiring
├── catalog/                  # Phase 1
│   ├── manifest.rs           # tome-catalog.toml (strict)
│   ├── store.rs              # registry persistence (atomic) — Phase 2 hooks cascade
│   └── git.rs                # git shell-outs + scrub_credentials
├── commands/
│   ├── catalog.rs            # tome catalog {add,remove,list,update,show}
│   ├── plugin/               # NEW — enable/disable/list/show; bare `tome plugin` → interactive
│   │   ├── mod.rs            # dispatch + shared helpers (aggregate_for_plugin, open_index_for_read, …)
│   │   ├── enable.rs         # `tome plugin enable`
│   │   ├── disable.rs        # `tome plugin disable` — confirm prompt + `--force`; non-TTY → exit 54
│   │   ├── list.rs           # `tome plugin list`
│   │   ├── show.rs           # `tome plugin show`
│   │   └── interactive.rs    # bare `tome plugin` — three-level loop + LoopExit
│   ├── query.rs              # NEW
│   ├── models/               # NEW — download/list/remove
│   │   ├── mod.rs            # dispatch + shared helpers (cheap_state, etc.)
│   │   ├── download.rs       # `tome models download` — indeterminate spinner; idempotent skip
│   │   ├── list.rs           # `tome models list [--verify]` — cheap probe / rehash via sha256_file
│   │   └── remove.rs         # `tome models remove <name> [--force]` — confirm + non-TTY exit 54
│   ├── reindex.rs            # NEW — `tome reindex [<scope>] [--force]` (Scope enum + run_with_deps lib entry)
│   ├── status.rs             # NEW — `tome status [--verify]` + `print_version(json)` for the --version pre-parse hook
│   └── workspace/            # NEW (Phase 3 US2) — `tome workspace {info,init}`
│       ├── mod.rs            # dispatcher; resolves Paths once and threads through
│       ├── info.rs           # `tome workspace info` — `assemble + emit` (silent compute + emit wrapper)
│       └── init.rs           # `tome workspace init` — thin wrapper over `workspace::init::init`
├── plugin/                   # NEW
│   ├── manifest.rs           # plugin.json (lenient)
│   ├── frontmatter.rs        # SKILL.md frontmatter (lenient + fallbacks)
│   ├── components.rs         # skills/agents/commands/hooks/.mcp.json walks
│   ├── identity.rs           # <catalog>/<plugin> address parsing
│   └── lifecycle.rs          # enable/disable orchestrator (atomic per plugin)
├── index/                    # NEW
│   ├── db.rs                 # rusqlite open, WAL, busy_timeout
│   ├── schema.rs             # CREATE TABLE statements (mirror of contracts/index-schema.sql)
│   ├── migrations.rs         # forward-only migrations under advisory lock
│   ├── vec_ext.rs            # sqlite-vec extension load (build.rs compiled)
│   ├── skills.rs             # CRUD on skills table; content-hash diff
│   ├── query.rs              # KNN search + reranker invocation
│   ├── meta.rs               # drift detection
│   ├── integrity.rs          # PRAGMA integrity_check
│   └── lock.rs               # advisory lockfile
├── embedding/                # NEW
│   ├── mod.rs                # Embedder + Reranker traits
│   ├── fastembed.rs          # fastembed-rs impl
│   ├── stub.rs               # #[cfg(test)] deterministic stub
│   ├── registry.rs           # MODEL_REGISTRY (pinned URLs + checksums)
│   ├── download.rs           # reqwest::blocking + SHA-256 + atomic persist
│   └── runtime.rs            # ort Environment, CPU EP only
└── presentation/             # NEW
    ├── tables.rs             # comfy-table helpers
    ├── progress.rs           # indicatif wrappers (TTY-aware)
    ├── colour.rs             # owo-colors + NO_COLOR
    └── prompt.rs             # inquire wrappers (refuse on non-TTY)

vendor/
└── sqlite-vec/               # NEW — pinned C source + LICENSE; compiled via build.rs

tests/
├── catalog_*.rs              # Phase 1 (catalog_remove extended for cascade)
├── manifest_strictness.rs    # Phase 1 (extended: model manifest is strict)
├── path_validation.rs        # Phase 1
├── exit_codes.rs             # extended for the 18 Phase 2 codes
├── scrubbing.rs              # extended for model URL scrubbing
├── atomicity.rs              # extended for enable / model download interrupts
├── plugin_{enable,disable,list,show,interactive,repeated}.rs   # NEW (interactive uses `rexpect`; disable + repeated drive CLI binary)
├── query.rs                  # NEW
├── models_{download,list,remove}.rs                   # NEW
├── reindex.rs                # NEW
├── status.rs                 # NEW
├── catalog_update_reindex.rs # NEW
├── catalog_remove_cascade.rs # NEW
├── concurrency.rs            # NEW — two-process index contention
├── schema_migrations.rs      # NEW
├── version_output.rs         # NEW
├── frontmatter.rs            # NEW — table-driven parser cases
└── fixtures/
    ├── sample-catalog/       # Phase 1
    └── sample-plugin/        # NEW
```

## Recent Changes

- 2026-05-27: **Phase 5 / US3 closed across 2 PRs (#119 + US3.d closeout). Substitution layer is now fully operational end-to-end (all 4 stages wired).** Slice progression: **#119 (US3.a+b)** Stage 3 (argument substitution: `$ARGUMENTS`, `$ARGUMENTS[N]`, `$N`, `$name`) + Stage 4 (`ARGUMENTS:` append-fallback footer). Picked **Option A (structural)**: extended the US2.d combined_regex with 4 Stage 3 patterns + 3 more capture groups (6 total) instead of running Stage 3 as a separate sweep — this means **NFR-007 no-rescan invariant enforced structurally across all 3 stages** (Stage 1↔2 was US2.d's fix; Stage 1↔3, 2↔3, 3↔3 all inherit via single-sweep). Capture-group constants: `ENV_NAME_GROUP=1`, `BUILTIN_NAME_GROUP=2`, `DEFAULT_GROUP=3`, `ARG_INDEX_GROUP=4`, `POSITIONAL_GROUP=5`, `NAMED_GROUP=6`. Bare `$ARGUMENTS` recognised by `m.as_str() == "$ARGUMENTS"`. F3's `apply_arguments` stub replaced; `ARGUMENTS_RE` slot removed (subsumed by combined_regex). New: `shell_split` (inline R-10 algorithm, no shell-words dep), `coerce_arguments` (6-row caller-coercion table: Single shell-split, Object named lookup, partial Object → empty fill, catch-all `args` key, mismatch → `PromptArgumentMismatch` exit 26), `apply_arguments_match`, `bare_arguments_value`. Stage 4 newline-aware append-fallback (`\n\nARGUMENTS: <value>`, format per contract verbatim). **0-indexed positional convention** (`$0` = first; matches contract + Claude Code). 45 new tests across `tests/substitution_arguments.rs` (28: 12 patterns + 3 NFR-007 invariant + 5 Stage 4 + 8 lib shell_split unit + 9 regex_sets group pins). 3 pre-existing tests updated for active Stage 3+4 (substitution_skeleton + mcp_prompts). **US3.c-1 (commit 35fa06d)** end-to-end pipeline test via `tests/entry_e2e.rs` (4 tests: enable → search → get_skill with substitution; enable command → prompts/get with named args; prompts/list user_invocable filter; prompt_argument_mismatch surfaces correctly). T269/T270/T271 verification confirmed no wiring fix needed — the merged single-sweep regex from US3.a+b automatically propagated through both `get_skill` and `prompts/get`. Test delta 1134 → 1138. **US3.d closeout (this PR)** — 4-reviewer pass surfaced **0 BLOCKERS + 4 majors + 1 test gap + 5 minors** (CLEANEST PASS of Phase 5 so far — structural fixes from US2.d inherited automatically). Applied 2 majors + 1 test. **R-M1**: replaced substring `body.contains("$ARGUMENTS")` heuristic (false-positive on `$ARGUMENTS_HELP` / `$ARGUMENTS_SUFFIX`) with new `substitution::body_has_bare_arguments` public helper using production regex dispatch + capture-group inspection. **R-M2**: deleted dead `_entry_identity_from_record` (zero callers, no future-consumer comment). **T-M1**: new `stage_2_substituted_value_containing_dollar_pattern_is_not_rescanned_by_stage_3` pinning the Stage 2↔3 boundary explicitly (third NFR-007 invariant test alongside existing Stage 1↔3 + Stage 3↔3 coverage). Deferred per `review/us3-disposition.md`: R-M3 (coerce_arguments clone micro-opt), R-M4 (empty-Object library-API error), 5 Rust minors, 3 test edge cases (newlines in arg values, unmatched quotes, JSON error envelope shape coverage). **Closeout**: `/sdd:map incremental` refreshed all 8 codebase docs via 4 parallel mappers; `retro/P5.md` filled (Option A structural fix over Option B separate sweep; reviewer pass severity correlates inversely with structural-fix history — US1.d 1 BLOCKER + 8 majors → US2.d 2 BLOCKERS + 6 majors → US3.d 0 BLOCKERS + 2 majors-applied; inline fixes when reviewer pass is small ≤5 file edits / 30 LOC; tests/entry_e2e.rs as e2e validation pattern; 0-indexed positional convention worth flagging in v0.5.0 release notes; EnvVarGuard + ENV_MUTEX at 4 file-local copies — promote at 5th consumer); this CLAUDE.md update. **Test total 1089 → 1139 across 141 → 143 suites; ignored 16 unchanged.** No new top-level dependencies. **Phase 5 cumulative test delta so far: 954 → 1139 (+185) across 127 → 143 suites.** **Substitution layer is now COMPLETE** — 4 stages wired through single-sweep regex, NFR-007 enforced structurally, exfiltration vector permanently closed. US4 (middle-tier discovery + when_to_use indexing) opens next. See `retro/P5.md`.

- 2026-05-27: **Phase 5 / US2 closed across 3 PRs (#116–#117 + US2.d closeout). Substitution layer paths/env operational end-to-end + CRITICAL data exfiltration vector caught and fixed by reviewer pass.** Slice progression: **#116 (US2.a)** Stage 1 (built-ins) + clock injection + path sanitisation. `BUILTIN_REGEX` compiled (`\$\{TOME_([A-Z0-9_]+)(?::-(.*?))?\}`); 12-built-in whitelist (`SKILL_DIR`/`SKILL_PATH`/`SKILL_NAME`/`PLUGIN_DIR`/`PLUGIN_NAME`/`PLUGIN_VERSION`/`PLUGIN_DATA`/`CATALOG_NAME`/`WORKSPACE_NAME`/`WORKSPACE_DATA`/`DATE`/`TIMESTAMP`); `sanitise_path_component` (non-`[A-Za-z0-9._-]` → `_`); `ensure_plugin_data` + `ensure_workspace_data` lazy `create_dir_all` + override-seam consultation; first production use of `PluginDataDirWriteFailed` (exit 9 from US1.d). `current_clock()` promoted as public free function reading `SUBSTITUTION_CLOCK_OVERRIDE` then falling back to `now_utc()` (note: `now_local()` requires the `local-offset` cargo feature not enabled — US1.c precedent). 21 new tests across `tests/substitution_builtins.rs`. **#117 (US2.b)** Stage 2 (env passthrough) + workspace-rename data-dir relocation. `ENV_REGEX` compiled (`\$\{TOME_ENV_([A-Z0-9_]+)(?::-(.*?))?\}`); `apply_env` runs `replace_all` with default-value fallback per FR-031, `tracing::debug!` on unset-no-default per FR-032, `Cow::Borrowed` fast-path; non-`TOME_ENV_` references (e.g. `${GITHUB_TOKEN}`) pass through unchanged per FR-033 + NFR-005. `src/workspace/rename.rs` extended to relocate `<home>/.tome/workspaces/<old>/plugin-data/` → `<home>/.tome/workspaces/<new>/plugin-data/` after SQL UPDATE commits; failure surfaces `WorkspaceDataDirWriteFailed` (exit 25); silent no-op if source absent. 16 new tests across `tests/substitution_env.rs` (9: 4 host-env state rows + non-TOME_ENV_ namespace + idempotence + mixed stages), `tests/substitution_data_dir.rs` (6: lazy create + idempotence + concurrent-safe), `tests/workspace_rename.rs` (1 new). Per-file `ENV_MUTEX` + `EnvVarGuard` RAII helper serialises env mutation across parallel tests (Phase 4 / US3.c-1 `harness_sync_stub.rs` precedent). **US2.c (T225-T227 / commit ad59254)** wires `get_skill` MCP tool to invoke `substitution::render()` before returning body (was only `prompts/get` from US1.c). Reuses `current_clock()`; re-reads plugin.json for `plugin_version` (acceptable per-call cost; US3 may optimise). 4 new tests across `tests/substitution_pipeline.rs` (stage-ordering, idempotence, end-to-end through both MCP surfaces). 1080 → 1084 (+4 tests across +1 suite). **US2.d closeout (this PR)** — 4-reviewer pass surfaced **2 BLOCKERS + 11 majors + 15+ minors**; applied 2 blockers + 6 majors. **B2 (BLOCKER — DATA EXFILTRATION VECTOR)**: `apply_env` was running `replace_all` over the FULL Stage 1 output, violating NFR-007 / FR-051's no-rescan invariant. A hostile plugin author could set `"version": "${TOME_ENV_GITHUB_TOKEN}"` in `plugin.json` (lenient parser — `src/plugin/manifest.rs:21`) and any skill body using `${TOME_PLUGIN_VERSION}` would leak the operator's env var into LLM context. **Other affected Stage 1 fields**: `entry_path`, `entry_dir`, `plugin_root_dir`, `skill_path`, `entry_name`, `catalog_name`, `plugin_name`. **Fix**: merged Stage 1 + Stage 2 into a single regex sweep using union pattern `\$\{TOME_(?:ENV_([A-Z0-9_]+)|([A-Z0-9_]+))(?::-(.*?))?\}` with leftmost-first alternation (env branch wins). New `regex_sets::combined_regex()` replaces separate `builtin_regex()` + `env_regex()`. Per-match resolution loop in `render()` resolves each match in isolation; resolved values NEVER re-scanned. NFR-007 now structurally enforced. **B1 (BLOCKER)**: `WORKSPACE_DATA` error path was wrapping `SubstitutionError::PluginDataDirCreationFailed` (exit 9) — fixed to `WorkspaceDataDirCreationFailed` (exit 25). Branch is unreachable from production (workspace name flows from validated `WorkspaceName::as_str()`). **Majors applied**: R-M1/R-M3/R-M5 subsumed by B2's single-sweep refactor; R-M2 drop dead `plugin_data_dir` + `workspace_data_dir` fields from `SubstitutionContext` (resolution goes via `data_dir::ensure_*` directly); R-M4 `caps.get(N).expect(...)` over `.unwrap_or("")` (defensive against future regex changes); T-M1 new no-rescan invariant test (`stage_1_substituted_value_containing_tome_env_syntax_is_not_rescanned_by_stage_2`) exercising the exact exfiltration scenario from B2 + 2 additional defence tests. Deferred per `review/us2-disposition.md`: 5 majors (C-M1 subsumed; C-m1/C-m2 doc-only; R-S1-S6 cosmetic; T-M2/T-M3+ edge cases; 2 Security MEDIUM env-exfil-via-legit-TOME_ENV/Unicode-sanitisation; 2 Security LOW symlink-check-in-rename/0600-mode). **Closeout**: `/sdd:map incremental` refreshed all 8 codebase docs via 4 parallel mappers; `retro/P4.md` filled (single-regex-sweep beats two-regex-sweeps when no-rescan is in spec; structural-fix-over-targeted-fix for security-critical invariants; reviewer pass MUST run before any user-facing security-sensitive surface ships; sub-agent dispatch verification pattern reaffirmed; `EnvVarGuard` + `ENV_MUTEX` rule-of-3 promotion deferred until US3); this CLAUDE.md update. **Test total 1039 → 1089 across 137 → 141 suites; ignored 16 unchanged.** No new top-level dependencies. **Phase 5 cumulative test delta so far: 954 → 1089 (+135) across 127 → 141 suites.** US3 (argument substitution + Stage 3 + Stage 4) opens next; the merged single-sweep regex from US2.d means US3 inherits the no-rescan invariant structurally. See `retro/P4.md`.

- 2026-05-26: **Phase 5 / US1 closed across 5 PRs (#110–#114 + US1.d closeout). Commands surface as MCP prompts end-to-end.** Slice progression: **#110 (Phase 1 Setup)** planning artefacts + version bump 0.4.0→0.5.0-dev. **#111 (Phase 2 Foundational)** F1 5 new `TomeError` variants + exit codes 25–29 (contract amended inline — originally proposed 21/22/23 collided with Phase 2 plugin lifecycle, so Phase 5 cluster is now clean at 25–29); F2 no-op (`regex` already direct since Phase 1); F3 `src/substitution/` module skeleton (7 files: mod/context/builtins/env/arguments/data_dir/regex_sets) + 3 RAII test guards (`ClockOverrideGuard`/`PluginDataDirGuard`/`WorkspaceDataDirGuard` mirroring `HarnessModulesGuard` pattern with `PoisonError::into_inner` recovery) + 7 smoke tests. **#112 (US1.a)** schema v2→v3 migration (kind/searchable/user_invocable/when_to_use columns); EntryKind enum (Skill | Command); widened lenient `SkillFrontmatter` with custom `deserialize_arguments` (space-string OR YAML list); `resolved_searchable(kind)` + `resolved_user_invocable(kind)` helpers; commands directory walk in `plugin::components`; EntryKind plumbed through `lifecycle::enable`; `embedding_text` composer includes `when_to_use`; KNN filters `searchable=1`. Two beneficial side effects: (a) framework FK toggle promoted to `apply_pending` because SQLite silently ignores `PRAGMA foreign_keys` inside a transaction — Phase 4 migration ALSO benefits; (b) SQLite 12-step table-rebuild pattern for the unique-constraint widening (the contract's literal `DROP INDEX IF EXISTS skills_unique` no-ops against auto-named `sqlite_autoindex_*`). 23 new tests across `schema_migration_v3.rs` (6), `frontmatter_p5_fields.rs` (10), `entry_kind_indexing.rs` (7). **#113 (US1.b)** MCP prompts capability — `prompts/list` + name derivation + collisions. T122 rmcp 1.7.0 verification confirmed `#[prompt_router]`/`#[prompt]`/`#[prompt_handler]` macros + `PromptRouter<S>` + `PromptRoute<S>` exist (no hard-stop), with 4 deviations documented in `notes/rmcp-prompts-api.md`: (1) alias `rmcp::model::Prompt` directly as `PromptDescriptor` (no parallel type); (2) dynamic registration via `PromptRoute::new_dyn` not `#[prompt_router]` macro (Tome prompts aren't compile-time-known); (3) no `PromptListResponse` wrapper needed; (4) `PromptsCapability` declared via typestate builder (`#[non_exhaustive]`). New modules: `mcp::prompt_name` (derive_name + sanitise + sanitise_trunc; PLUGIN_PORTION_MAX=16, ENTRY_PORTION_MAX=32, SEPARATOR="__"); `mcp::prompt_collision` (EntryIdentity + CollisionRecord + resolve_collisions, tie-break on (catalog,plugin,kind,name) lex order, counter-suffix from 2); `mcp::prompts` (PromptRegistry built at MCP startup querying enabled-AND-user-invocable entries, descriptor list truncated to 300 chars per FR-066). `PromptRoute::new_dyn` HRTB lifetime wrinkle resolved via free `fn stub_future<'a, S>(...)` whose return type binds the input lifetime. `#[prompt_handler]` macro NOT usable (expects compile-time prompts); manual `list_prompts` + `get_prompt` `ServerHandler` methods inside the existing `#[tool_handler] impl`. 34 new tests across `prompt_naming.rs` (10), `prompt_collision.rs` (6), `mcp_prompts.rs` (8), `mcp_prompts_list_json_shape.rs` (1) + 9 new lib unittests. Surfaced two latent bugs to address in US1.c. **#114 (US1.c)** real `prompts/get` handler replacing US1.b stub: resolves prompt → reads body via `spawn_blocking` → builds `SubstitutionContext` (12 built-ins) → runs `substitution::render()` (F3 stub returns body unchanged) → wraps in `GetPromptResult { description (300-char cap), messages: [PromptMessage::new_text(User, rendered)] }`. Arg mapping per FR-041–FR-043 (named→Object, no-args+`args` key→Single, mismatch→PromptArgumentMismatch). MCP error envelopes for 4 failure modes. **Latent-bug fix**: promoted US1.b's private `resolve_plugin_dir_for_row` helper to `index::skills::resolve_entry_body_path` consumed by both `prompts/get` AND `get_skill` tool (whose pre-US1.c `PathBuf::from(&row.path)` treated `row.path` as absolute when it's actually relative — bug never surfaced because no existing test exercised the file-read branch). New `Paths::plugin_data_dir_for` + `workspace_data_dir_for` (anchored under `<home>/.tome/`; create_dir_all lands in US2). `time::now_local()` requires the `local-offset` cargo feature (not enabled) — used `now_utc()` as default with `SUBSTITUTION_CLOCK_OVERRIDE` test seam. 8 new tests across `mcp_prompts.rs` extensions (7) + `mcp_prompts_get_json_shape.rs` (1). **US1.d closeout (this PR)** — 4-reviewer pass (contract/Rust-lens/test/security) surfaced **1 BLOCKER + 13 majors + 16 minors**; applied 1 blocker + 8 majors. **S-H1 (BLOCKER)**: path traversal in `resolve_entry_body_path` — function joined `stored_path` (DB-supplied relative path) to `plugin_dir` without `..` validation; a hostile plugin could craft `../../../etc/passwd`. Fixed: rejects `..` components AND absolute paths via `Path::components()` check. **R-M1**: split new `PluginDataDirWriteFailed` (exit code **9**) from `WorkspaceDataDirWriteFailed` (25) — closed-error-enum discipline (the substitution-side already has two distinct variants; the From impl was collapsing them). **R-M2 + S-L1**: route frontmatter parse failures at `prompts/get` time through `SkillFrontmatterParseError` (23) — was being stuffed into `EntryNotFound.kind` breaking the discriminator contract and leaking raw paths into error messages. **R-M3 + R-M4**: cache `plugin_version` on `PromptEntry` at registry build (one SELECT addition); drop the second DB open + lossy `display().to_string()` path round-trip from `render_for_get`. **R-M5**: drop dead `Box<SkillRecord>` from `get_skill::LookupHit`. **S-M1**: cap `arguments` list at 256 (DoS protection at plugin enable). **T-M1 + T-M5**: byte-stable JSON wire-shape pins for all 5 error envelopes (new `mcp_prompts_get_error_json_shape.rs`). **T-M3**: registry-build degradation tests (entry file missing on disk, frontmatter malformed). Deferred per `review/us1-disposition.md`: C-M1/C-M2 (contract polish), R-M6 (`Arc<PromptEntry>` optimization), T-M2 (substitution-error path — F3 stub blocks, lights up naturally in US2/US3), S-M2 (YAML panic safety), 16 minors. **Closeout**: `/sdd:map incremental` refreshed all 8 codebase docs via 4 parallel mappers; `retro/P3.md` filled with US1 learnings (latent-bug surfacing via slice composition, contract amendments inline, pre-allocation slice archetype, 4-reviewer parallel-pass discipline, byte-stable JSON pin discipline, MAX_ARGUMENTS=256 size cap at parse boundary, mapper-refresh-at-phase-boundary, sub-agent vs orchestrator division-of-labour); this CLAUDE.md update. **Test total 954 → 1039 across 127 → 137 suites; ignored 16 unchanged.** No new top-level dependencies. New deps via F1: PluginDataDirWriteFailed (exit 9), 5 Phase 5-cluster variants (25–29). Two latent bugs fixed (get_skill abs/rel path; closed-set discipline on data-dir failure variants). See `retro/P3.md`.

- 2026-05-26: **Phase 5 planning complete (spec + plan only; no code change yet).** `/sdd:specify` produced `spec.md` (5 user stories, 64 FRs / 12 NFRs / 13 SCs; 5 new exit codes 21–23 + 25–26; zero `[NEEDS CLARIFICATION]` markers after Rust-lens review pass landed 3 blockers + 8 majors + 12 minors + 6 suggestions inline). Spec validation: requirements.md checklist all-passing. `/sdd:plan` produced `plan.md` (Phase 5 adds zero new top-level deps; `regex` promoted from transitive to direct; binary-size delta projected < 1 MiB), `research.md` (20 R-decisions covering hand-rolled substitution engine, regex strategy via `OnceLock`-cached compiled sets, schema migration v2→v3 with backfill defaults per FR-111a, prompt naming algorithm with `<plugin>__<entry>` format + counter-suffix collision handling, MCP prompts capability with `listChanged: false`, persistent data directory layout under central tree, frontmatter parsing for the widened lenient field set, argument coercion shell-style quoting matching Claude Code's documented behaviour, test injection seams for clock/registry/data-dir overrides per the Phase 4 P6 lesson, JSON wire-shape pin discipline carried forward from Phase 4 P8 retro, per-US 4-reviewer pass pattern carried forward, pre-emptive slice plans per US per Phase 4 P3 lesson, Phase 4 deferred items disposition), `data-model.md` (Schema v3 DDL + Rust types for EntryRow/EntryKind/SubstitutionContext/SubstitutionContextBuilder/ArgumentValues/SubstitutionError + MCP tool I/O types + PromptDescriptor/PromptRegistry/CollisionRecord + Doctor extension structs + 5 new TomeError variants), 9 contracts (exit-codes-p5, schema-migration-p5, frontmatter-p5, substitution-engine, mcp-tools-p5, mcp-prompts, entry-schema-p5, doctor-extensions-p5, catalog-and-plugin-extensions-p5), and `quickstart.md` (end-to-end 13-step walkthrough verifying every SC). Constitution gate: PASS, no deviations. Pre-emptive slice plan: 3 Foundational PRs (F1 pre-allocate 5 error variants + exit codes; F2 promote `regex` to direct dep; F3 `src/substitution/` module skeleton + Stub impl + override seams) then US1 (commands as MCP prompts, 4 slices), US2 (substitution layer paths/env, 3 slices), US3 (argument substitution, 3 slices), US4 (middle-tier discovery + when_to_use indexing, 3 slices), US5 (per-entry invocability + doctor extensions, 3 slices), then Polish phase mirroring Phase 4's PR-A→H pattern. Codebase docs (`.sdd/codebase/`) refreshed earlier today against Phase 4 complete (954 tests, 127 suites); not re-run for Phase 5 spec (no code change). Phase 5 reviewer findings on the spec (R-1–R-12 substantive + S-1–S-6 suggestions): all addressed inline. Useful context loaded during planning: Claude Code MCP docs (https://code.claude.com/docs/en/mcp.md — confirmed `/mcp__servername__promptname` slash format, `list_changed` capability semantics, stdio transport env-var inheritance) and Claude Code Skills docs (https://code.claude.com/docs/en/skills.md — confirmed `user-invocable` IS a real Claude Code frontmatter field with default `true`; Tome deliberately defaults skills to `false` to avoid slash-menu pollution; confirmed `$ARGUMENTS` / `$N` / `$name` substitution patterns; confirmed "substitution runs once over the original file" semantics matching FR-051).
- 2026-05-26: **Phase 4 Polish phase closed across 6 PRs (#103–#108) + this closeout PR. v0.4.0 SHIPPED.** Phase-wide 4-reviewer parallel pass surfaced 3 blockers + 43 majors + 50 minors/nits; consolidated to `specs/004-phase-4-refactor-harnesses/review/{findings,disposition}.md`. PRs landed: **#103 (PR-A)** reviewer findings + disposition. **#104 (PR-B)** 3 blockers — C-B1 exit code 20→24 sweep across 3 contract docs + spec.md; C-B2 `InitOutcome` JSON envelope rename (`workspace_dir`→`path`, `inherited_catalogs`→`catalogs_inherited`, new `id` field) to match `workspace-commands.md`; C-B3 doctor `harnesses[].name` underscored→hyphenated (`claude_code`→`claude-code`) so all doctor harness fields use the same form. **#105 (PR-C)** 9 selected majors — R-M3 `HARNESS_MODULES_OVERRIDE` PoisonError::into_inner recovery; R-M8 `harness::lookup` demoted to `#[doc(hidden)] pub`; R-M12 `workspace::rename` same-name routes through `WorkspaceNameInvalid` (exit 15, not Usage exit 2); C-M9 data-model `harness_rules`/`mcp` align to impl's named-struct shape; C-M1 new `HarnessModule::detect_path(&home)` trait method (default + `claude-code` override) fixing the `~/.claude-code/` wire lie; R-M4 dropped duplicate `ProjectMarkerConfig` from `workspace::resolution`; R-M5 promoted canonical `settings::parser::read_project_marker` collapsing 3 readers + 2 inline pairs (IO failures now exit 7, parse failures exit 70); R-M7 new `src/index/workspaces.rs` with `resolve_id_required`/`_optional` consolidating 9 `SELECT id FROM workspaces` sites; C-M12 doctor binding-broken `SuggestedFix` split into two entries (rebind / recreate). **#106 (PR-D)** test coverage gaps — T-M6 JSON wire-shape pins for `EffectiveEntry`+`AsWrittenOutcome` (new `tests/harness_list_json_shape.rs` 6 tests), `SyncOutcome` family (new `tests/sync_outcome_json_shape.rs` 7 tests), `WorkspaceCatalogEntry` (extends `tests/workspace_info.rs` +2); T-M9 doctor JSON Phase-4 field pins + byte-stable minimal report; T-M7 CLI binary exit-code coverage for codes 14/16/17/18/70/7 in `tests/exit_codes_e2e.rs`; T-M10 scrubbing extensions for summariser URL + harness MCP error chains. **#107 (PR-E)** security hardening (the biggest Polish PR) — S-M1 new `src/util/io.rs::bounded_read_to_string` with per-class size caps (`TOME_CONFIG_MAX=1MiB`, `PLUGIN_MANIFEST_MAX=256KiB`, `HARNESS_MCP_MAX=1MiB`, `HARNESS_RULES_MAX=4MiB`) migrated to ~26 production read sites refusing over-cap with `Io(InvalidInput)`; S-M2 `util::atomic_dir::land_directory*` refuses symlink targets + `.old` aside cleanup; S-M6 `doctor::orphan_cleanup::sweep_one` adds `symlink_metadata` check before `is_dir()` mirroring `mcp/tools/get_skill::walk_dir`; S-M7 both `home_root()` implementations validate `$HOME` is set + absolute + canonical (error type changed: relative/unset is now `Usage` exit 2, not `Io` exit 7); T-M8 `tests/paths_phase{2,3}.rs` collapsed per-file `EnvGuard`/`ENV_LOCK` onto shared `common::HomeGuard` + `HOME_MUTEX` (2 `unsafe` blocks removed); T416 `tests/security_hardening.rs::all_tome_owned_writes_emit_0o600_on_unix` audit; T419 `RELEASE-BINARY-SIZE.md` records 26.32 MiB on macOS arm64. **#108 (PR-F)** docs + release — Cargo.toml 0.3.0→0.4.0; CHANGELOG `[0.4.0]` entry; README "Phase 4 in flight"→"Phase 4 shipped (v0.4.0)"; constitution v1.3.0 §Paths amendment ratified 2026-05-22 (no edit needed). **Closeout (PR-G)** — `/sdd:map incremental` refreshed all 8 codebase docs via 4 parallel mappers; `retro/P8.md` filled (phase-wide 4-reviewer parallel pass pattern, sub-agent dispatch for refactors, per-class size caps + helper-on-every-read pattern, symlink refusal at every entry point, `home_root` validation chain, mid-phase mapper rotation recommendation for Phase 5+, JSON wire-shape pin discipline from day one); this CLAUDE.md update. **Test total 916 → 954 across 125 → 127 suites; ignored 16.** No new top-level dependencies. Deferred to v0.5.0 backlog per `review/disposition.md`: C-M2 (Paths canonicalisation), C-M4-7/M10-11 (contract doc-only), R-M1/M2/M6/M9-11 (consolidation), S-M3 (prompt-injection design), S-M4 (--fix --force dry-run), S-M5 (summariser lock), S-M8 (llama-cpp-2 traceability), T-M1/M2/M3/M4/M5 (fixture promotion), T-M11. Phase 4 feature work + Polish complete; Phase 5 unscoped.
- 2026-05-26: **Phase 4 / US5 closed across 4 PRs (#99–#101 + closeout). ALL FIVE PHASE 4 USER STORIES NOW COMPLETE; only Polish remains.** `tome doctor` extended end-to-end. Slice progression: **#99 (US5.a)** Phase 4 doctor subsystems — `Subsystem` enum promotion (11 variants with byte-stable wire), `SubsystemHealth` enum, `ProjectBindingState`, `RulesCopyState`, `assemble_report` extensions (project_binding / summariser / effective_harness_list / harness_rules / harness_mcp / detected_uninstalled_harnesses), classification per FR-561 (Summariser/Binding broken → Unhealthy; BindingRulesCopy/HarnessRules/Mcp → Degraded; empty list → NotApplicable). 32 new tests across doctor_p4.rs, doctor_subsystem_serialize.rs, doctor_detected_uninstalled.rs, doctor_read_only_by_default.rs (FR-563), mcp_input_length_caps.rs (T373 4096-char query cap). T376b 5×9 per-method harness matrix in harness_modules.rs. **#100 (US5.b)** `--fix` handlers + `--force` flag + orphan cleanup — all five Phase 4 repair classes (Summariser, BindingRulesCopy, HarnessRules, HarnessMcp, Schema); `--force` override for user-owned MCP entries; `src/doctor/orphan_cleanup.rs` (5-layer defence-in-depth removal of stale `.tome.tmp.*` staging dirs older than 1 hour); exit codes 0/1/75; new `filetime = "0.2"` dev-dep for mtime-backdate tests. **#101 (US5.c-1)** reviewer-pass fixups: 4-agent review surfaced **1 blocker + 21 majors**; PR applied 1 blocker + 10 majors. Headline fixes: `SubsystemHealth::NotApplicable` emitted as per-harness entries when outside-project (C-M1 — was declared but unused); `check_index` softened to graceful Broken collapse on `SchemaTooNew(52)` per FR-561 "doctor never crashes" (C-M2); new `workspace::sync::sync_one_project` for project-local repair (C-M3 — was workspace-broadcasting via `sync_one`); `--force` without `--fix` → Usage exit 2 (R-M1); harness sync coalesced once per project per run (R-M2 — was 10× redundant); `RulesCopyState::SourceMissing` variant distinguishes absent-source vs absent-copy (R-M5 — prevents `--fix` infinite-loop); `repair_catalog` uses `open_read_only` (R-M7); `--force` precisely scoped to in-play UserOwned HarnessMcp entries (S-M2); `debug_assert!(cache_dir.starts_with(&paths.catalogs_dir))` safety invariant (S-M4); 3 CLI binary e2e tests for exit 75 / `--fix --force` rewrite / `--force` without `--fix` (T-M1); summariser-fix dispatch test (T-B1). Deferred: R-M3 (re_assemble drift), R-M4 (cosmetic), R-M6 (TOCTOU doc), S-M1 (project_path canonicalisation), S-M3 (unbounded reads carry forward), T-M2/M3/M4/M5/M6/M7 (test gaps). **Closeout (US5.c-2)** — `/sdd:map incremental` refreshed all 8 codebase docs via 4 parallel mappers; `retro/P7.md` filled (typed Subsystem enum pattern, NotApplicable per-row state, coalesce-repair-invocations, project-local vs broadcast naming, distinct variants for distinct failure modes, --force precise scoping, debug_assert on DB-derived paths, 5-layer remove_dir_all defence-in-depth); this CLAUDE.md update. **Test total 862 → 916 across 117 → 125 suites; ignored 16.** New dev-dep: `filetime = "0.2"`. See `retro/P7.md`. **Phase 4 feature work fully complete; Polish phase opens per `tasks.md`.**
- 2026-05-26: **Phase 4 / US4 closed across 5 PRs (#94–#97 + closeout).** Bundled local-LLM summarisation ships end-to-end. Slice progression: **#94 (US4.a)** production `LlamaSummariser` (`llama-cpp-2 = "=0.1.146"` + `encoding_rs = "0.8"`); model load + SHA-256 verify; inference sampling (temperature 0.3 / top_p 0.9 / repeat_penalty 1.1); length-window enforcement; `tome models` extension (summariser as third model); byte-progress callback on download. 6 unit tests + 1 env-gated `summariser_real.rs` test. **#95 (US4.b)** trigger wiring across 4 sites (enable/disable/reindex/catalog-update); `SUMMARISER_OVERRIDE` slot + `SummariserOverrideGuard` test helper; MCP `search_skills` description includes cached short via `mcp::tool_description::compose`; FR-385 forward-progress (mutation commits before summariser; failure exits 24 with mutation retained); `ModelMissing` silent-noop carve-out in triggers (pragmatic for on-demand model). 19 new tests across `summariser_triggers.rs`, `summariser_forward_progress.rs`, `summariser_cache.rs`, `mcp_tool_description.rs`, `plugin_summariser_forward_progress.rs` (F11c-2 placeholder lit up). **#96 (US4.c)** env-gated real-model `regen-summary` E2E + exit-24 CLI binary coverage. **#97 (US4.d-1)** reviewer-pass fixups: 4-agent review surfaced **5 blockers + 21 majors**; PR applied all 4 actionable blockers + 9 majors. Headline fixes: **real Qwen SHA-256 obtained** (`74a4da8c…d7a9db`; F6 shipped placeholder, US4.a's brief didn't flip it — entire US4 production path was non-functional until this fix; regression test `tests/summariser_registry_no_placeholder.rs` pins the constant); stale "exit 20" doc drift scrubbed to "exit 24" across 3 files + contract; `LONG_MAX_CHARS` consolidated into single source (was 2400/2500 split across `prompts.rs` and `regen_summary.rs` causing double-warn); end-to-end trigger test via `SummariserOverrideGuard` (the slot existed but was untested via the production code path — added `tests/summariser_triggers_end_to_end.rs`); `LlamaSummariser` caches `LlamaModel` after first verify (per-trigger 400 MB re-hash eliminated); `summarise::backend()` uses `PoisonError::into_inner` recovery (no more brick-on-poison); `tome catalog remove --force` cascade now triggers regen (was the one workspace_skills mutation site missing the trigger); `format_input_descriptions` no longer prefixes `"- "` (matched contract); `LlamaSummariser::new` rejects placeholder SHA explicitly (belt-and-braces); `summariser_entry()` helper replaces `.expect` on `.find()`; `tracing_subscriber` in-test layer for warn-capture; `ModelMissing` silent-noop contract clarification. Deferred per `review/us4-disposition.md`: prompt-injection trust boundary doc (S-M1), per-description length cap (S-M2 pairs with deferred US2.d-1 S2-M3), rmcp private-field reach (R-M1), various rust cosmetic (R-M3/M4/M5/M8), test gaps (T-M1/M3/M4). **Closeout (US4.d-2)** — `/sdd:map incremental` refreshed all 8 codebase docs via 4 parallel mappers; `retro/P6.md` filled (F6 → US4.a registry placeholder oversight as root cause + lessons on skeleton-to-production wiring audits, exit-code grep discipline, length-constant single-source promotion at first cross-module consumer, end-to-end test for any new test-injection seam in same PR); this CLAUDE.md update. **Test total 825 → 862 across 110 → 117 suites; ignored 16.** New deps: `llama-cpp-2 = "=0.1.146"`, `encoding_rs = "0.8"`. See `retro/P6.md`.
- 2026-05-25: **Phase 4 / US3 closed across 6 PRs (#88–#92 + closeout).** Layered settings + composition resolver + `tome harness` command surface ship end-to-end. Slice progression: **#88 (US3.a)** composition resolver verification + 19 tests (priority walk, composition expansion, cycle detection, FR-449 invariant). Production fix: DFS cycle path renders walk-order chain via parallel `visited: HashSet + stack: Vec` structures (was alphabetically-sorted). **#89 (US3.b)** validation rules: `[workspace]`-outside-project + bracketed-exclusion + harness-not-supported + TOML array-type strictness. 18 new tests. **#90 (US3.c-1)** cross-harness module tests (70 tests across 5 modules) + race fix for `HarnessModulesGuard` + `MutexGuard` tuple-drop order (mutex was dropping FIRST, leaving a window where another test installed its own override before the outgoing test's guard cleared it; macOS stable surfaced the flake first). **#91 (US3.c-2)** full `tome harness` command surface (bare/list/use/remove/info/sync) — 6 subcommand modules, new `settings::edit` module wrapping `toml_edit::DocumentMut` for surgical TOML edits, scope-arg parsing via clap `ValueEnum`, 22 new tests. **#92 (US3.d-1)** reviewer pass: 4 agents (contract/Rust/test/security) surfaced **6 blockers + 29 majors**; PR applied all 6 blockers + 12 selected majors. Headline fixes: production `harness::sync::sync_project` no longer uses `StubScope::new()` — replaced with `CentralDbScopeProvider` consulting central DB for workspace membership + reading workspace's settings.toml for directly-declared harnesses (C-B1/C-B2/S-M1/S-M4/R-M1, the F8 skeleton's production wiring was the single critical issue); `tome harness info` now reports composition references via `Vec<HarnessReference { scope, via }>` (C-B3); `tome harness use|remove` acquire `index.lock` advisory lockfile around the settings-file read-modify-write window (C-M5/R-M2/S-M2); per-entry unsupported-harness validation in `resolve_list` (`["fake", "!fake"]` errors immediately on inclusion, doesn't silently cancel via exclusion); HOME mutation race in tests serialised via `HOME_MUTEX` + `HomeGuard` RAII helper (T-B1); CLI subcommands refactored to expose `assemble_*` silent-compute entry points so tests can assert on returned Outcome instead of stdout side-effects (T-B2); per-test-file `OVERRIDE_MUTEX` for `harness_skeleton.rs` (T-B3); JSON wire-shape pins for `HarnessBareEntry`/`HarnessInfoOutcome`/`HarnessUseOutcome`/`HarnessRemoveOutcome` (T-M2); `tests/security_hardening.rs` extended with mode preservation + symlink refusal for `settings::edit::save_settings` (S-M3). Deferred per `review/us3-disposition.md`: C-M3 (bare MCP heuristic), C-M7 (harness use notice differentiation), C-M8 (cycle path prefix — depends on C-M1 fix anyway), C-m2 (info detected_path multi-word), R-M4/M5/M6/M7 (cosmetic Rust), T-M4/M7/M9. **Closeout (US3.d-2)** — `/sdd:map incremental` refreshed all 8 codebase docs via 4 parallel mappers; `retro/P5.md` filled with US3 learnings (F8 skeleton-to-production wiring gap, tuple-drop discipline now established for `HarnessModulesGuard` + `HomeGuard`, `CentralDbScopeProvider` 4-state distinction pattern, per-entry validation principle, `assemble_*` + `run` silent-compute/emit-wrapper retroactively applied to all 6 `commands::harness::*`); this CLAUDE.md update. **Test total 677 → 825 across 92 → 110 suites; ignored 17.** No new dependencies; `toml_edit` now consumed by 4 sites (`harness::mcp_config`, `workspace::regen_summary`, `workspace::rename`, `settings::edit`). See `retro/P5.md`.
- 2026-05-25: **Phase 4 / US2 closed across 6 PRs (#82–#86 + closeout).** Full workspace-lifecycle command surface (`init/list/info/rename/regen-summary/remove/sync`) shipped end-to-end. Slice progression: **#82 (US2.a-1)** init + list + info — Phase 4 name-keyed `init <name> [--inherit-global]` replaces Phase 3's path-based init; `list` with per-workspace counts (catalogs / plugins / skills / bound projects / last_used_at); `info [<name>]` extended with enrolled catalogs + enabled plugins + bound projects + summary cache state. **#83 (US2.a-2)** rename + regen-summary — `rename <old> <new>` walks bound projects to rewrite each marker's `workspace = "<new>"` line; `regen-summary <name>` is the first production consumer of the `Summariser` trait (`StubSummariser` in tests; `LlamaSummariser` BackendInitFailed until US4.a); writes `[summaries]` table via `toml_edit`, rewrites RULES.md from long summary, propagates to bound projects via new `workspace::sync::sync_workspace_rules_to_bound_projects` helper. **#84 (US2.b)** remove + 5-step cascade — `remove <name> [--force]` runs the FR-405 cascade under the advisory lock: per-project integration teardown → per-project marker dir removal → single DB tx (skill/catalog/project/workspace rows) → central workspace dir removal → refcount-clean catalog caches. Bound projects without `--force` → exit 16. **#85 (US2.c)** sync CLI — `tome workspace sync [<name>]` wraps the helper from US2.a-2 with per-workspace + all-workspaces iteration; promotes `sync_workspace_rules_to_bound_projects` → `sync_one` with rich `WorkspaceSyncOutcome { synced_projects, unchanged, missing_project_dirs }`. **#86 (US2.d-1)** reviewer fixups — 4-agent review pass produced 4 blockers + 23 majors; PR #86 applied all 4 blockers + 11 selected majors. Headline fixes: per-project effective list narrowing in remove cascade (C-B1; previously iterated global `SUPPORTED_HARNESSES` unconditionally); SQL UPDATE opens transaction BEFORE marker rewrite loop in rename (C-B2 / R-M2); bare array (no envelope) for `workspace list --json` (C-B3); `toml_edit::DocumentMut` for marker rewrite in rename preserves the optional `harnesses` field (T-B1 / C-M3 / R-M4); mode preservation + symlink refusal lifted into `catalog::store::write_atomic` (S2-M1 / S2-M2); rename recovery branch chmod 0o700 (S2-M4); JSON wire-shape pins for `InitOutcome` / `RenameOutcome` / `RegenSummaryOutcome` / `RemoveOutcome` (T-M1); cascade Step-1 coverage for rules-file teardown + user-owned MCP entry preservation (T-M3); regen-summary preserves `[[catalogs]]`/`harnesses` test (T-M5); `workspace_list` non-zero count test (T-M6). Deferred per `review/us2-disposition.md`: R-M1/R-M3/R-M5/R-M6/R-M7/R-M8 (cosmetic Rust); S2-M3 (unbounded reads — multi-site change for v0.4 polish); T-M2 (concurrent init/rename/regen tests — pattern established, defer to test hardening). **Closeout (US2.d-2)** — `/sdd:map incremental` refreshed all 8 codebase docs via 4 parallel mappers; `retro/P4.md` filled with US2 learnings (cross-check contract TOML samples against deserialisation strictness; "Inside ONE DB transaction" contract claims need clarification when they conflate FS + SQL; per-project effective-list narrowing pattern is now established for US3.c/US5 reuse); this CLAUDE.md update. **Test total 609 → 677 across 82 → 92 suites; ignored 17.** No new dependencies; `toml_edit` is now used in 3 places (`harness::mcp_config`, `workspace::regen_summary`, `workspace::rename`); `Summariser` trait exercised in production for the first time. See `retro/P4.md`.
- 2026-05-25: **Phase 4 / US1 closed across 8 PRs (#74–#80 + closeout).** `tome workspace use <name> [--force] [--json]` ships end-to-end against the real claude-code harness module. Slice progression: **#74 (US1.a)** core binding flow — `bind_project` runs under the advisory lock, UPSERTs `workspace_projects` keyed on canonical `project_path`, updates `workspaces.last_used_at` in the same transaction, lands `<project>/.tome/` atomically via `util::atomic_dir::land_directory_with_replace`. `is_project_root_acceptable` refuses cwd-is-home/cwd-is-root with exit 2 (`--force` bypasses); `WorkspaceNotFound` exit 13 for unknown workspace. **#75 (US1.b-1)** rules-file write primitives — `parse_block` regex on `<!-- tome:begin --> / <!-- tome:end -->` line markers; `write_block` + `write_standalone` with idempotence-by-mtime, symlink refusal, atomic-write discipline. **#76 (US1.b-2)** mcp_config primitives — JSON branch via `serde_json` (with `preserve_order` feature) + TOML branch via `toml_edit::DocumentMut`. `is_tome_owned` ownership marker `command == "tome" && args[0] == "mcp"`; `env` field preserved across Tome-owned rewrites per FR-503. **#77 (US1.b-3)** sync orchestrator (`harness::sync::sync_project`) — computes effective harness list via `settings::resolver::resolve_effective_list` (`StubScope` for now; production DB-backed provider lands in US3.a), dispatches per-harness writes through the `HarnessModule` trait, runs the cleanup pass for harnesses no longer in the effective list, dedupes multi-harness shared rules-file and MCP-config paths. `HarnessClash` → exit 19 with FR-403 forward-progress (binding remains committed even on sync failure). `HARNESS_MODULES_OVERRIDE` + `HarnessModulesGuard` RAII helper for synthetic-registry tests; per-test-file `OVERRIDE_MUTEX` for intra-binary serialisation. **#78 (US1.c)** claude_code production harness — module already shipped in F7; US1.c added 13 unit tests + 5 end-to-end tests against the real `SUPPORTED_HARNESSES` registry. Verified row-by-row against research §R-8. **#79 (US1.d-1)** test surface expansion — cross-product bind pre-state, FR-403 forward-progress, atomic-dir orphan handling, concurrent bind via `std::sync::Barrier`, CLI binary exit-code spec pinning. **#80 (US1.d-2a)** reviewer fixups — 4-agent review pass (contract / Rust-lens / test / security) produced 3 blockers + 25 majors; PR #80 applied all 3 blockers + 9 selected majors. Headline fixes: refuse non-UTF8 project paths up front (R-B1, lossy `to_string_lossy` into `workspace_projects` PK); wrap UPSERT + last_used_at in `conn.transaction()` (R-M1, contract FR-411); preserve original file mode across atomic rewrite via `symlink_metadata` capture + chmod staged tempfile before persist (S-M3, prevents silent 0644 → 0600); contract amendment in `mcp-config-integration.md` § Ownership marker clarifying `env` preservation applies only to Tome-owned rewrites (T-B1); created `tests/sync_idempotence.rs` (T-B2). Deferred items tracked in `review/us1-disposition.md`. **Closeout (US1.d-2b)** — `/sdd:map incremental` refreshed all 8 codebase docs via 4 parallel mappers; `retro/P3.md` filled with US1 learnings; this CLAUDE.md update. **Test total 490 → 609 across 64 → 82 suites; ignored 29.** No new dependencies. Two new error variants used in anger (`HarnessClash` exit 19, `CompositionError` exit 17 via the StubScope); both pre-allocated at F3. See `retro/P3.md`.
- 2026-05-23: **Phase 4 / Foundational closed across 5 PRs (#68–#72).** F10 (#68 `e565ad1`) shipped `WorkspaceName` newtype with FR-347 validation + `Scope(WorkspaceName)` tuple struct + `ResolvedScope.project_root: Option<PathBuf>`; deleted `--global` CLI flag, `WorkspaceMarkerMissing` (71), `WorkspaceConflict` (72); rewrote resolver with central-DB membership check + `--workspace <NAME>` semantics; mechanical sweep across 35 files. F11 scope-lift (#69 `9a36e12`) parameterised every hard-coded `WHERE name = 'global'` SQL literal — 12 sites across `src/index/skills.rs`, `src/index/query.rs`, `src/commands/status.rs`, `src/commands/plugin/mod.rs`, `src/plugin/lifecycle.rs`; `LifecycleDeps::workspace_name(&self)` accessor at the rule-of-3; cascade-disable for catalog now uses `mark_all_disabled_for_plugin` (junction delete) per FR-383 retention rule, not `delete_by_plugin` (cross-workspace skill drop); new `tests/plugin_cheap_reenable_across_workspaces.rs` proving FR-006 holds across workspaces. F11b (#70 `caaf32f`) rewired all catalog commands onto the `workspace_catalogs` junction — new module `src/index/workspace_catalogs.rs` (302 LOC) with `insert`/`delete`/`find`/`list_for_workspace`/`distinct_urls`/`refcount_by_url`/`workspaces_with_catalog_url`/`resolve_catalog_path`; **Option B architectural decision**: derived secondary metadata (last_synced from clone-dir mtime; path from `paths.cache_dir_for(&url)`; plugin_count from cached manifest) instead of duplicating into the new schema; `Config.catalogs` no longer written (transitional shell). FR-362 add now refcount-aware (clone vs reuse) under advisory lock; FR-363 remove + cache cleanup atomic under same lock per FR-366; FR-367 concurrent-remove returns benign `CatalogNotFound`. F11c-1 (#71 `79380f7`) shipped catalog test coverage: new `tests/catalog_workspace_refcount.rs` (T098o, 5 tests covering FR-361/366/367) + `tests/catalog_update_cross_workspace_reindex.rs` (T098s); deleted `tests/catalog_cache_refcount.rs` (5 ignored Phase 3 tests subsumed by new); deleted `catalog_add::config_toml_is_chmod_0600_on_unix` (premise gone with F11b); rewrote `catalog_remove_cascade::cascade_remove_in_workspace_does_not_remove_shared_clone` + renamed `catalog_update::refresh_all_runs_alphabetically` → `refresh_all_is_deterministic`. F11c-2 (#72 `d695782`) added `tests/plugin_workspace_skills.rs` (T098p, 4 tests covering FR-380/381/383 isolation + retention) + `tests/plugin_summariser_forward_progress.rs` placeholder (T098l + T098r, US4.b unhide-target); extended `tests/manifest_strictness.rs` (T098n FR-348 audit) — surfaced zero missing `#[serde(deny_unknown_fields)]` across Phase 4 types; extended `tests/schema_migration_e2e.rs` with MVCC reader-during-writer + `SchemaVersionTooNew` after future-version migration (T098m FR-325). **Production-code edits in F11c-2**: one line — `// not-strict` marker on the inner `#[serde(untagged)]` `Repr` enum in `src/settings/mod.rs:103` (`untagged` is mutually exclusive with `deny_unknown_fields`; grep guard needs the opt-out marker). **Test total 449 → 490 across 59 → 64 suites; ignored 33 → 29.** Codebase docs (all 8) refreshed via `/sdd:map incremental` with 4 parallel mapper agents. See `retro/P2.md` for the full F1–F11 retro (workarounds, patterns, dependency notes, for-next-time).
- 2026-05-14: **Phase 4 planning complete (specs + plan only; no code change yet).** `/sdd:specify` produced `spec.md` (5 user stories, 110 FRs / 9 NFRs / 20 SCs across 5 user stories + Foundational; 8 new exit codes 13–20; reused-variant table for failure modes mapping to Phase 1–3 codes per FR-602; one constitution amendment required — v1.3.0 §Paths). `/sdd:plan` produced `plan.md` (Phase 4 adds two direct deps — `llama-cpp-2` for Qwen2.5-0.5B-Instruct summariser inference, `toml_edit` for comment/order-preserving harness-MCP-config edits — plus `serde_json/preserve_order` feature flag; drops `directories` crate; projected binary size ~28.4 MiB on macOS arm64, ~34 MB on Linux x86_64, well under the 50 MB cap), `research.md` (19 R-decisions covering home-resolver-via-std::env, llama-cpp-2 sync+singleton, Qwen2.5-0.5B-Instruct selection, binary-size projection, toml_edit + serde_json/preserve_order, schema v1→v2 structural-only migration, per-harness specifics for 5 harnesses, composition TOML-strings-not-table-headers, atomic-populated-directory helper promotion, new module structure, constitution v1.3.0 amendment, pre-emptive slice plans per user story, Phase 3 deferred-items disposition, StubSummariser shape, prompts + length windows, project-directory definition for `workspace use`, test discipline carry-over), `data-model.md` (Paths reshape under `<home>/.tome/`, Scope reshape to WorkspaceName, central DB schema v2 DDL including PK on `workspace_projects.project_path` alone for 1:1 binding, settings file shapes, HarnessModule trait, Composition resolver types, Summariser trait + LlamaSummariser + StubSummariser, 8 new TomeError variants, DoctorReport extensions with Subsystem enum promotion, atomic_dir helper signature, CLI surface additions/removals), 13 contracts (paths-and-layout-p4, workspace-commands, harness-commands, sync-algorithm, harness-modules, rules-file-integration, mcp-config-integration, settings-composition, summariser, doctor-extensions-p4, catalog-and-plugin-extensions-p4, schema-migration-p4, exit-codes-p4), and quickstart.md (end-to-end Phase 4 smoke test). Rust-lens review on the spec surfaced 8 blockers + 11 majors + 12 minors; all blockers and 9/11 majors resolved in-spec before plan started. Constitution gate: PASS with two deviations documented in Complexity Tracking (bundled summariser as the third inference runtime; the v1.3.0 §Paths amendment landing in Foundational F1). Pre-emptive slice plan: 10 Foundational PRs (F1 constitution amendment, F2 drop directories, F3 pre-allocate 8 error variants, F4 atomic_dir promotion, F5 toml_edit + serde_json/preserve_order, F6 summariser skeleton + StubSummariser + byte-progress download, F7 harness skeleton, F8 settings/composition, F9 schema migration registration, F10 WorkspaceName + project_path PK) then US1 (bind a project, 4 slices), US2 (workspace lifecycle, 3), US3 (layered settings + composition, 3), US4 (summarisation + RULES.md, 3), US5 (doctor extensions, 2), then Polish phase mirroring Phase 3's PR-A→H pattern. Codebase docs refreshed via `/sdd:map incremental` (4 mappers in parallel against the Phase 3-complete tree post-PR-A→H polish). Per the P10 retro recommendation, the plan explicitly carries 7 Phase 3 deferred items into Foundational + user-story slices (read-only DB open refactor → F2; MCP input length caps → US5; fabricate_models rename → F6; subsystem enum promotion → US5.a; synthetic SuggestedFix injection drop → F9; download_model byte-progress callback → F6; `Paths.config_file` field rename → resolved-by-removal in F2). Three items pushed to Phase 5+ (workspace prune; manual SC-001/SC-002 against real models; T093/T094/T095 MCP integration tests).
- 2026-05-14: **Phase 3 Polish phase shipped across 8 PRs (#51–#58) — Phase 3 closes and ships as v0.3.0.** Pre-release review pass (4 reviewers in parallel: contract audit, Rust-lens code review, test audit, security audit) surfaced 3 blockers + ~25 majors + ~55 minors. Triage in `review/findings.md` + `review/disposition.md`. **PR-A** (#51) — review artefacts. **PR-B** (#52) blocker B1 — `mcp.log` JSON field names (`tracing-subscriber` default emits `timestamp`/`message`; contract pins `ts`/`msg`). Custom `ContractEventFormat` (`FormatEvent` impl) renders contract-pinned names; 5 new tests in `tests/mcp_log_format.rs`. **PR-C** (#53) blockers B2+B3 — resolver enforces `workspace-resolution.md` §Validation 1b/1c (malformed config / unopenable index → `WorkspaceMalformed` 70) at every entry path; doctor embedder + reranker drift classification get explicit tests. 9 new tests across `workspace_resolution.rs` (+6) and `doctor.rs` (+3). **PR-D** (#54) MCP majors — SIGTERM handler (Unix only), 5 s graceful-shutdown timeout, `error!` "hard shutdown" event, `error!` preflight-failure event, credential scrubbing on `workspace_path` + `error_message` log fields, `search_skills.filter` Debug → named fields, index-DB-missing → `IndexIntegrityCheckFailure` (51, the closed-enum authority — the `exit-codes-p3.md` contract typo'd it as 35). **PR-E** (#55) doctor majors — new `CatalogCacheState::Orphan` variant + on-disk `catalogs_dir` walk; `WorkspaceRegistryStatus` field + reader; `fixes::apply` signature → infallible `usize`; `build_suggested_fixes` emits `subsystem: "schema"` when `index.schema_version < SCHEMA_VERSION` (lights up `repair_schema` which was dead code). 3 new tests in `doctor.rs`. **PR-F** (#56) security hardening — `mcp.log` 0600 on Unix via `OpenOptionsExt::mode`; `get_skill` `walk_dir` skips `is_symlink()` entries (hostile-catalog `skills/foo/creds -> ~/.ssh/id_rsa` defence); workspace registry validation (1 MiB size cap, 10k entry cap, NUL + `..` rejection); `init` refuses non-directory `.tome` marker; `init --force` propagates pre-cleanup errors; registry dedupe by canonicalize. 7 new tests in `security_hardening.rs`. **PR-G** (#57) deferred coverage — T219 `embedding_registry_structs_all_carry_deny_unknown_fields` grep guard + M-MIG-2 sequential-not-concurrent test rename. **PR-H** (this PR) — `exit-codes-p3.md` contract typo fixed (35 → 51); `doctor.md` synopsis updated with `--verify`; README Phase 3 section; CHANGELOG `[0.3.0]` entry naming all new commands, 8 new exit codes (60/61/70/71/72/73/74/75), 3 new direct/transitive deps (`rmcp`, `tokio` scoped to `src/mcp/`, `schemars`); `Cargo.toml` 0.1.0 → 0.3.0; P8 retro fill. **374 → 394 tests across 51 → 53 suites.** Binary size: 22 MiB on macOS arm64 (well under the 50 MB cap). Tracked deferrals: T088 manual SC-001 / SC-002 verification against real BGE models; T093/T094/T095 MCP protocol-purity + latency + SIGINT tests (need real models or a stub-injection point); the rest of `m-WKS-*` long-tail test gaps. See `retro/P8.md`.
- 2026-05-14: **Phase 3 / US5 (forward schema migrations) shipped across 2 commits on `003-phase-3-us5`. All five Phase 3 user stories now ship; feature work complete.** `eaeb22b` initialised the P7 retro shell. `009f7b6` (US5.a — fixtures + e2e + atomicity + doctor `--fix` interaction): **test-only changes — no production code modifications**. The migration framework itself (`index::migrations::apply_pending`, `Migration { from, to, name, apply }`, `MIGRATIONS_OVERRIDE` thread_local) shipped in Foundational F7 already. New `tests/schema_migration_e2e.rs` (5 tests) covers the four cases from `contracts/schema-migration.md` §Testing strategy via `MIGRATIONS_OVERRIDE` injection: single-step forward, multi-step forward, mid-sequence failure leaves last-good intermediate, newer-on-disk refused with `SchemaVersionTooNew` (exit 73); a fifth test verifies the `MigrationsGuard` Drop-clears-slot discipline. `tests/atomicity.rs` extended with `migration_abort_mid_transaction_…` modelling the SIGINT mid-tx scenario as a deliberate `Err` from the migration closure — `catalog::git::CANCELLED` deliberately NOT used (would race other tests in the binary; same discipline as `atomicity_enable.rs`). `tests/doctor.rs` extended with `fix_runs_forward_schema_migration_end_to_end` (now 13 tests) — bootstraps a real v1 DB with the production registry seeds (so `check_drift` passes), downgrade-stamps to v0, injects a synthetic `subsystem: "schema"` `SuggestedFix` (Phase 3's `build_suggested_fixes` doesn't emit one because zero migrations are registered), runs `doctor::fixes::apply` + `re_assemble`, verifies the marker table created inside the migration is present on disk + classification flips to `Ok`. New helper `tests/common/mod.rs::write_index_db_with_schema_version(path, version)` fabricates a minimal `meta`-only synthetic DB at the requested version — generate-at-setup convention; no binary `.db` files committed. **No new dependencies. No new error variants. No production code changes.** Phase 3 still ships `pub const MIGRATIONS: &[Migration] = &[]`; the first real migration lands in Phase 4+. Test total 367 → 374 across 50 → 51 suites. Codebase docs refreshed via `/sdd:map incremental` (4 mappers in parallel). See `retro/P7.md`.
- 2026-05-14: **Phase 3 / US4 (`tome doctor`) shipped across 5 commits on `003-phase-3-us4`.** `18d66f4` initialised the P6 retro shell. `b120e74` (US4.a — report assembly + per-subsystem checks library API): new `src/doctor/` module — `mod.rs::assemble_report(scope, paths, home, verify) -> DoctorReport` is the silent compute entry; `report.rs` carries `DoctorReport` + `CatalogCacheHealth` + `CatalogCacheState` (Ok/Missing/NotARepo/ManifestInvalid) + `HarnessPresence` + `DoctorClassification` + `SuggestedFix` per data-model §5; `checks.rs::check_catalogs` enumerates the resolved scope's catalogs and classifies each on-disk clone (existence + `.git/` + manifest-parses gates); `harness_detect.rs::probe(home)` probes six well-known harness dirs (`.claude` / `.codex` / `.cursor` / `.gemini` / `.opencode` / `.continue`) with existence-only checks per FR-167; `home: &Path` is a parameter so tests can isolate `$HOME` against a tempdir without env mutation. `commands::status::check_model` / `check_index` / `check_drift` promoted from private to `pub` so doctor reuses them — single source of truth across the two diagnostic surfaces. Classification: Unhealthy on embedder failure / index integrity fail / embedder drift; Degraded on reranker failure / reranker drift / any catalog cache state != Ok; Ok otherwise. 7 lib unit tests. `b526cef` (US4.b — `--fix` + CLI surface): `fixes.rs::apply(&mut DoctorReport, paths, scope)` runs every `auto_fixable: true` suggested fix in order, routed by `subsystem` string (`"embedder"` / `"reranker"` / `"catalog:<name>"` / `"schema"`); each repair clears prior state then calls the underlying library (`embedding::download::download_model`, `Git::clone_shallow`, `migrations::apply_pending` under advisory lock); per-subsystem check re-runs inline after each repair updating the report in place; `re_assemble(&mut report)` rebuilds `suggested_fixes` + `overall` without re-probing catalogs/harnesses; `has_remaining_manual_fixes` is the exit-75 predicate. `src/commands/doctor.rs` is the thin CLI wrapper: resolves `$HOME`, calls `assemble_report`, optionally applies `--fix` + `re_assemble`, emits human (TTY glyph + ASCII fallback) or JSON, handles exit codes 0 / 1 / 75. `Command::Doctor(DoctorArgs { fix, verify })` wired in `src/cli.rs` + `src/main.rs`. `f3bf09b` (US4.c — tests): `tests/doctor.rs` 12 tests (failure classes, `--fix` repair of broken catalog cache, exit codes 0/1/75, harness detection, --global override) + `tests/doctor_json.rs` 3 tests (JSON envelope shape pinned + broken-catalog suggested_fixes + missing-models unhealthy). The catalog re-clone repair is exercised end-to-end via a real `Fixture` (file:// git repo); model re-download isn't exercised in CI (would fetch real BGE weights from HuggingFace) — library-level coverage stays at `cheap_state` / `check_model`. Closeout commit refreshes all 8 codebase docs + retro fill + CLAUDE.md. Test total 345 → 352 (lib) → 367 (integration) across 48 → 50 suites. No new dependencies. No new error variants (`DoctorFixNotSafe` / code 75 was already in the closed enum from F1). See `retro/P6.md`.
- 2026-05-14: **Phase 3 / US3 (per-command scope honouring + reference-counted catalog clone cleanup) shipped across 4 commits on `003-phase-3-us3`.** `c098b8f` initialised the P5 retro shell. `1f98f2c` (US3.a — cross-product scope-isolation coverage): 9 new tests in `tests/workspace_commands.rs` covering catalog/plugin/status across workspace vs global, including bootstrap-on-first-write verification (`catalog::store::save`'s existing `create_dir_all(parent)` and `index::open`'s file+parent creation already handle the workspace case — no production code changes; the integration test is the verification artefact). T131–T142 were already complete from Foundational F1; T143's "refactor" commit folds into this test commit because there's no refactor code to commit. `7b1c907` (US3.b — feat): `catalog::store::reference_count(url, paths) -> Vec<Scope>` walks the global config + every workspace in `${state_dir}/workspaces.txt` (opt-in; absent = empty list); doc comment captures TOCTOU profile per contract §Concurrency. `commands::catalog::add` restructured to allow same-URL-across-scopes: if `cache_dir.exists()` reuse the existing clone (re-parse manifest from cache, skip git, fall through to per-scope config write); a `reuse_existing: bool` local controls the rollback path so we only `remove_dir_all` a cache we cloned ourselves. `commands::catalog::remove` calls `reference_count(&entry.url, &paths)` after `store::save`; only `fs::remove_dir_all(&entry.path)` when refs is empty; otherwise logs `still_referenced_by` at debug. Cascade-disable + refcount compose implicitly (cascade runs inside `remove::run` and falls through). The Phase 1 cache-collision error (`CatalogAlreadyExists(... cache path collision)`) is no longer reachable. `bc8c827` (US3.c — refcount tests): 5 new tests in `tests/catalog_cache_refcount.rs` (shared-clone-across-scopes, conditional cleanup, registry-absent fallback degradation, sequential race emulation) + 1 new cross-scope cascade test in `tests/catalog_remove_cascade.rs` that bridges library-API enable (StubEmbedder) and CLI binary cascade (pure deletion — bridges the stub-vs-real seed-space cleanly because cascade-disable doesn't construct an embedder). Closeout commit refreshes all 8 codebase docs and the P5 retro. **No new dependencies.** **No new error variants.** Test total 330 → 339 → 345 across 46 → 47 → 48 suites. See `retro/P5.md`.
- 2026-05-14: **Phase 3 / US2 (workspace creation + inspection) shipped across 3 commits on `003-phase-3-mcp-workspaces`.** `9bffc08` initialised the P4 retro shell. `8a1ed87` (US2.a — `tome workspace info`): `Command::Workspace(WorkspaceArgs)` + nested `WorkspaceCommand::Info` in CLI; `src/commands/workspace/{mod,info}.rs` mirrors the `commands/plugin/` dispatcher pattern; `WorkspaceInfo` / `ScopeKind` / `ModelIdentity` types live in `src/workspace/info.rs` (re-exported from `mod.rs`); `commands::workspace::info::assemble(scope, paths) -> WorkspaceInfo` is the pure compute path (silent-compute/emit-wrapper pattern carried from US1.b — tests target `assemble`, the CLI uses `run`); catalog count comes from the resolved-scope config TOML (parse failure → `WorkspaceMalformed` 70); index facts come from a read-only SQLite handle (`open_read_only`) and absent fields collapse to zero counts + `None` schema/embedder. `ScopeSource` gained `#[derive(Serialize)]` + `#[serde(rename_all = "snake_case")]` so the `source` JSON wire-shape conforms to the contract (`flag` / `global_flag` / `env` / `cwd_walk` / `global_fallback`). 8 new tests in `tests/workspace_info.rs` including JSON byte-stability pin + malformed-config → exit 70 + two CLI binary smoke tests. `5664daf` (US2.b — `tome workspace init`): `WorkspaceCommand::Init(WorkspaceInitArgs)` lands; `src/workspace/init.rs::init(target_root, inherit_global, force, paths) -> InitOutcome` carries the atomic FS work; `src/commands/workspace/init.rs` is the thin emit wrapper; `src/workspace/inventory.rs::append_if_registry_exists` adds the opt-in registry append. Atomicity via `tempfile::Builder::new().prefix(".tome.tmp.").tempdir_in(&absolute)` (sibling staging dir = same-FS rename = POSIX-atomic) → 0700 chmod on Unix → populate `config.toml` via `catalog::store::write_atomic` → optional rename existing `.tome/` → `.tome.old/` → `TempDir::keep()` to consume the cleanup guard → `std::fs::rename(staged, marker)`; rollback restores `.tome.old/` if the final rename fails. Contract referenced `TempDir::persist` (doesn't exist as a method); `keep() + rename` gives the same atomicity guarantee. 12 new tests in `tests/workspace_init.rs` inc. concurrent two-thread init via `std::sync::Barrier::new(2)`, registry-opt-in/dedupe, `--force` sentinel-not-surviving, and three CLI smoke tests. No new dependencies (tempfile, serde, toml already used). No new error variants — reused `CatalogAlreadyExists` (4) for workspace-already-initialised per the contract's explicit green-light. Test total 310 → 318 → 330 across 44 → 45 → 46 suites. Codebase docs refreshed via `/sdd:map incremental` (4 mappers in parallel). See `retro/P4.md`.
- 2026-05-14: **Phase 3 / US1 (MCP server) shipped across 5 commits on `003-phase-3-mcp-workspaces`.** `92ba0db` initialised the P3 retro. `dc78f54` (US1.a — server scaffolding): `Command::Mcp(McpArgs)` CLI variant; `src/commands/mcp.rs` thin dispatch shell; `src/mcp/state.rs` carrying `McpState { embedder: Arc<dyn Embedder>, reranker: OnceCell<Arc<dyn Reranker>>, scope, paths, embedder_entry, reranker_entry }`; `src/mcp/server.rs` with `#[tool_router]` + `#[tool_handler]` macros over both tools; `src/mcp/tools/{mod, search_skills, get_skill}.rs` with `JsonSchema`-derived schemas + stub handlers; `src/mcp/mod.rs::run` wires runtime → file subscriber → preflight (on `spawn_blocking`) → `rmcp::serve_server(server, transport::stdio())` → `tokio::select!` over `running.waiting()` / `signal::ctrl_c()`; `src/main.rs` special-cases `Command::Mcp` to skip `logging::init` + the sync ctrlc handler (the global `tracing` registry only accepts one subscriber + the async signal handler would race with the sync one); `tests/mcp_lifecycle.rs` covers 5 deterministic exit-code paths (72 / 60 / 73 / 30 / 32). `be98fc1` (US1.b — search_skills handler): refactored `commands::query` to extract `pub fn pipeline(args, deps) -> Result<QueryOutcome, TomeError>` as the silent compute path; `run_with_deps` becomes a thin emit-wrapper. MCP handler validates `plugin_without_catalog` / `unknown_catalog` up front, lazy-loads the reranker via `tokio::sync::OnceCell::get_or_try_init` + `spawn_blocking` for the model load, dispatches the KNN+rerank pipeline inside its own `spawn_blocking`, maps `TomeError` to MCP error envelopes with contract-defined `data.code` strings. `e2ff93c` (US1.c — get_skill handler): index lookup distinguishes `unknown_plugin` (no rows for `(catalog, plugin)`) from `unknown_skill` (no row OR row with `enabled = 0`); reads SKILL.md via `plugin::frontmatter::parse_skill_frontmatter` (same parser as enable pipeline); walks the parent directory **recursively** (deviated from T088's "non-recursively" — the contract says recursive) collecting siblings sorted lexicographically. `20c0723` (US1.d — tests + closeout): `tests/mcp_server.rs` lands 8 library-API tests covering router introspection (exactly two tools), description contract wording, FR-108 identifier-leakage check, and handler input-validation paths. `#[tool_router(vis = "pub")]` added so integration tests can reach `Server::tool_router()`. T092 happy-path tests + T093 protocol purity + T094 latency + T095 SIGINT graceful shutdown deferred to **T088 manual SC-001 / SC-002 verification** (need either real BGE models or a refactor to expose seeds on `McpState` for stub injection). Added `schemars = "1"` direct dep + `rmcp` features `transport-io` + `schemars`; binary 20.94 MiB → 22.04 MiB on macOS arm64 (+1.10 MiB, rmcp now actually linked through `serve_server` + `transport::stdio`). Test total 297 → 310 across 42 → 44 suites.
- 2026-05-14: **Phase 3 Foundational closed — F1–F8 landed across two commits on `003-phase-3-mcp-workspaces`.** F1–F6 in commit `6a08b5b` (PR #44, merged): `Scope` type + per-scope `Paths` accessors with the `_for(&Scope)` suffix (field rename deferred → shelved as unnecessary churn); 8 new `TomeError` variants + exit codes 60/61/70/71/72/73/74/75; workspace resolution algorithm with explicit `WorkspaceConflict` (72) instead of clap's `conflicts_with` so the dedicated exit code wins over usage-error 2; mechanical command-surface refactor to plumb `ResolvedScope` through every `commands::*::run`; read-only DB open across read paths (`open_read_only` mirrors `open`'s signature; retains legacy `SchemaTooNew` (52) per F5); `query::run_with_deps` library entry point (folded Phase 10 deferral). F7 (commit `26d7984`): migration framework — `Migration { from, to, name, apply: fn(&Transaction) -> Result<(), TomeError> }` replaces Phase 2's `sql: &'static str`; `apply_pending(conn, current, target) -> Result<u32, TomeError>` emits `SchemaVersionTooNew` (73) on too-new and `SchemaMigrationFailed` (74) on apply errors per the Phase 3 contract; the "no migration registered" defensive guard stays at `IndexIntegrityCheckFailure` (51). `MIGRATIONS_OVERRIDE` is `#[doc(hidden)] pub static` rather than `#[cfg(test)]` so Phase 7's `tests/schema_migration_e2e.rs` (integration test, no cfg(test) visibility) can reach it. Read-path `open_read_only` keeps the legacy `SchemaTooNew` (52) gate — both gates name the same condition but route through different exit codes; documented in both docstrings. F8 (commit `1814e30`): new `src/mcp/` async island — `mod.rs` (sync entry point stub), `runtime.rs` (single-threaded tokio per research §R-2), `log.rs` (10 MiB atomic-rename rotation + JSON-lines `FileMakeWriter`/`LockedFile` adapter + tracing subscriber with stderr `error!`-only sibling for FR-222), `preflight.rs` (FR-110 pipeline: schema-version gate → drift detection → SHA-256 verify primary embedder artefact → eager-load `FastembedEmbedder`; reranker deferred per FR-109). Added `json` feature to `tracing-subscriber`; binary size held at 20.94 MiB on macOS arm64 (vs 20.91 MiB baseline — LTO still drops un-referenced rmcp until US1). Sync boundary enforced by `tests/sync_boundary.rs` (path-component exemption for every file under `src/mcp/`). Test total 257 → 297 across 39 → 42 suites. No new top-level dependencies (rmcp / tokio / tracing-subscriber json were either pre-existing or feature additions).
- 2026-05-14: **Phase 3 planning complete.** `/sdd:specify` produced `spec.md` (5 user stories, 41 FRs / NFRs, 15 SCs). `/sdd:plan` produced `plan.md` (3 new deps direct or transitive — `rmcp`, `tokio` scoped to `src/mcp/`, optional `schemars`; estimated +1.9 MB binary, well under the 50 MB cap). `/sdd:plan` continued through Phase 0 producing `research.md` (15 R-decisions covering rmcp/tokio shape, MSRV, binary size, log rotation, workspace discovery algorithm, refcount semantics, async boundary, and the Phase 10 deferred-item triage), and Phase 1 producing `data-model.md` (Scope type, Paths refactor, DoctorReport, MCP tool schemas, 8 new TomeError variants), 9 contracts, and the quickstart. The plan deliberately folds in two Phase 10 deferrals (read-only DB open across read paths; `query::run_with_deps` library entry point) because MCP's read-side reuses both. Async lives strictly inside `src/mcp/`; a structural test enforces. Schema-migration framework ships with zero registered migrations + a synthetic-fixture e2e test. The Phase 10 retro's recommendation to encode pre-emptive slice splits in the plan is honoured: research §R-15 names the exact slice shape per user story for `/sdd:tasks` to consume.
- 2026-05-13: **Phase 10 polish in flight — PRs #34–#40.** Post-feature-complete review pass (4 reviewers in parallel: contract audit, Rust-lens code review, test audit, security audit) surfaced 8 blocker-label + 32 major findings. Disposition in `review/findings.md` + `review/disposition.md`. Pre-Phase-10 PRs: #34 fixed `catalog remove --force` cascade per-plugin counts (signature minimised too aggressively in Phase 9; helper now returns `Vec<(String, u32)>`); #35 reconciled ~15 contract drifts (catalog-update NDJSON envelope, catalog-remove nested `removed` record, query no-prompt-by-design, plugin TTY predicate is stdin AND stdout, enable decline=8 vs disable decline=0, status MiB suffix + writer_pid claim dropped, exit-codes adds Internal/code-1 row); #36 security hardening (`catalog add` URL scrub before persistence, `config.toml` chmod 0600 on Unix, `.gitignore` extensions for harness state, URL_LOGIN regex widened to any RFC-3986 scheme); #37 refactor (status::classify parens, surfaced query_row errors as integrity_ok=false, removed `_force_*` unused-import idioms in 3 places, promoted `human_mb` to `presentation::format`). Phase 10 in-flight: #38 added `tests/concurrency.rs` (cross-process lock contention via CLI) + `tests/schema_migrations.rs` (forward-only boundaries inc. "no migration registered" guard + CLI exit 52); #39 added `tests/exit_codes_e2e.rs` (CLI-binary coverage for cheap codes 22/33/51/20/2 + coverage matrix doc) + `tests/model_download.rs` mid-stream-abort cleanup test for FR-053 (faithful proxy for SIGINT; the test discipline keeps `CANCELLED` static unmanipulated). Test total 240 → 257 across 36 → 39 suites. Reviewer findings deferred to Phase 11 / post-v0.2.0: read-only DB open refactor (security-audit major, low urgency after writer_pid claim dropped from contract); query --strict / drift / --no-rerank CLI-binary coverage (requires `commands::query::run_with_deps` library entry point); error-message Display tests for 10+ Phase 2 variants; ModelManifest strictness grep guard. Manual SC-001 / SC-002 against real BGE models still pending.
- 2026-05-13: Closed Phase 9 / User Story 7 across PRs #32–#33 (final feature phase of Phase 2). Slice 1+2 combined (PR #32) shipped `tome catalog remove` Phase 2 extensions per `contracts/catalog-extensions.md`. Refuse path: enabled plugins + no `--force` → exit 53 (`CatalogHasEnabledPlugins`, pre-existing variant from Phase 2 foundational, used in production for the first time). Cascade path: `--force` + enabled plugins → drop each plugin's `skills` + `skill_embeddings` rows inside ONE advisory-lock window (single-lock-per-batch — different from Phase 7's per-plugin reindex pattern, because catalog removal is semantically one user action). New library helper `lifecycle::cascade_disable_for_catalog(paths, catalog, plugins, embedder_seed, reranker_seed) -> u32` deliberately takes a narrower signature than `auto_disable_orphan` (no `LifecycleDeps`, no `Embedder`) because the cascade is pure deletion. Pre-check `enabled_plugins_for_catalog` runs WITHOUT the lock (readers don't block writers; the worst TOCTOU outcome is dropping an extra plugin's rows, still correct). JSON `removed` envelope extended with optional `cascade` array (`skip_serializing_if = "Vec::is_empty"`). 3 new tests in `tests/catalog_remove_cascade.rs` via the CLI binary (cascade is pure deletion — no `FastembedEmbedder` needed); enable in test setup uses library API + `StubEmbedder`. Closeout (PR #33) refreshes all 8 codebase docs and the P9 retro. Headline learnings: (a) single-lock-per-batch vs per-plugin atomicity now coexist as established patterns — pick based on whether the operation is semantically one action or N independent operations; (b) pre-allocated error variants in the closed enum (variant existed since Phase 2, used in Phase 9 — zero refactor cost); (c) helper signature minimisation (don't pass `LifecycleDeps` when only `(paths, seeds)` are used); (d) combine feat+tests in one PR when total < ~250 lines AND nothing in feat needs a deeper review than the tests provide. Test total 237 → 240 across 35 → 36 suites. No new dependencies. No new error variants.
- 2026-05-13: Closed Phase 8 / User Story 6 across PRs #29–#31. Slice 1 (PR #29) shipped `tome status [--verify] [--json]` as a read-only doctor / pre-flight per `contracts/status.md`. New `src/commands/status.rs` (~330 lines): `StatusReport { tome, embedder, reranker, index, drift, overall }` with `ModelHealth`, `IndexHealth`, `OverallHealth::{Ok, Degraded, Unhealthy}`. `pub fn run` emits + `std::process::exit(1)` for non-Ok; `pub fn assemble_report` is the library-API entry point that bypasses the exit for tests (same library-bypass pattern as `reindex::run_with_deps`). Classification policy: embedder missing/corrupt OR index integrity failure OR embedder drift → Unhealthy; reranker missing/corrupt OR reranker drift → Degraded; otherwise Ok. Reads model state via the now-pub `commands::models::cheap_state`, opens index read-only (never takes the advisory lock — by design FR-056), runs `PRAGMA integrity_check`, detects drift via `index::meta::detect_drift`. `--verify` rehashes each model's primary artefact via `embedding::download::sha256_file`. Extended `tome --version` per `contracts/version-output.md`: clap's auto handler disabled (`disable_version_flag = true` on Cli derive), pre-parse hook in `main.rs` scans `std::env::args` BEFORE clap dispatch — emits three-line plain text (`tome <v>` / `embedder: <name> <v>` / `reranker: <name> <v>`) by default, or the structured JSON record when `--json` is also passed. Identities are compile-time constants from `MODEL_REGISTRY`; a model bump auto-bumps the output. Helper promotions slice 1: `ModelState`, `cheap_state`, `read_manifest`, `primary_file_path`, `human_mb` in `commands/models/mod.rs` from `pub(crate)` to `pub`. Slice 2 (PR #30) shipped 14 integration tests across `tests/status.rs` (10 tests: 7 library-API via `assemble_report` + 3 CLI binary for exit-code semantics) and `tests/version_output.rs` (4 tests: plain text + JSON + flag-order-irrelevant + `-V` short flag). Slice 2 also promoted `commands::plugin::registry_seeds` from `pub(crate)` to `pub` so drift tests bootstrap meta with BGE seeds (matching what status reads on the configured side); the StubEmbedder stays decoupled — seed records identity, embedder produces vectors. Test total 223 → 237 across 33 → 35 suites. No new dependencies. No new error variants.
- 2026-05-13: Closed Phase 7 / User Story 5 across PRs #25–#28. Slice 1 (PR #25) added `index::skills::reindex_plugin_atomic` (mirrors `enable_plugin_atomic`; classifies each skill as Added/Modified/Removed/Unchanged inside one SQLite transaction; honours `force` to re-embed unchanged rows) + `ReindexSummary` struct. Added `plugin::lifecycle::reindex_plugin(id, deps, force) -> ReindexOutcome` and `plugin::lifecycle::auto_disable_orphan(id, deps) -> u32` (drops all rows for a plugin whose upstream is gone — FR-033). Fixed a latent bug: `skill_embeddings` is a `sqlite-vec` virtual table that doesn't support `INSERT OR REPLACE` / `ON CONFLICT`, so `upsert_skill` now does `DELETE`-then-`INSERT`. The bug was unreachable from the existing enable path; reindex's Modified branch caught it. 9 new unit tests. Slice 2 (PR #26) added `enabled_plugins_for_catalog` query helper + `pub fn reindex_catalog_plugins(catalog, enabled, deps) -> CatalogReindexOutcome` in `commands/catalog/update.rs`, wired into `run()`. Lazy `FastembedEmbedder` loading via a `GetOrInsertWithResult` helper trait — a zero-enabled-plugin install never touches ONNX models on `tome catalog update`. SHA-pinned catalogs skip the reindex pass (intentionally frozen). Auto-disable cascades on `PluginNotFound` / `PluginManifestParseError`; other errors propagate. `commands/catalog` module promoted to `pub mod` so the test crate can reach `update::reindex_catalog_plugins`. 3 new integration tests via library API + `StubEmbedder`. Slice 3 (PR #27) shipped `tome reindex [<scope>] [--force]`: new `src/commands/reindex.rs` with `Scope` enum (All / Catalog / Plugin), `ReindexAggregate` outcome, `execute(...)` + `pub fn run_with_deps(...)` library entry point for tests. Same lazy-embedder pattern. Per-plugin atomicity (each `lifecycle::reindex_plugin` acquires its own advisory lock — a Ctrl-C between plugins leaves earlier ones committed, by design). 7 new tests (4 library + 3 CLI binary). Library/CLI test boundary now applies a third time: heavy-state paths → library + `StubEmbedder`; light/error paths → CLI binary. The pattern is firm. Test total 204 → 213 (slice 1) → 216 (slice 2) → 223 (slice 3) across 33 suites. No new dependencies. No new error variants. No new top-level error codes (the contract's exit codes 30/31/34/35/36/50/51/52 are already enumerated for the embedder/index domain).
- 2026-05-13: Closed Phase 6 / User Story 4 across PRs #22–#24. Slice 1 (PR #22) added the `tome models download | list | remove` CLI surface as a new `src/commands/models/` module mirroring the `plugin/` layout (`mod.rs` dispatch + per-subcommand files); wires the already-shipped `embedding::download::download_model` library, the pinned `MODEL_REGISTRY`, and `presentation::{tables, progress, prompt}`. Promoted `embedding::download::sha256_file(path) -> Result<String, TomeError>` to `pub` (streaming chunked SHA-256, used by `models list --verify` and likely Phase 7 reindex). Relaxed `output::write_json` to `T: Serialize + ?Sized` so slice serialisation works without an intermediate `Vec` allocation. Indeterminate `indicatif` spinner ships on both `plugin enable` and `models download` — byte-progress callback refactor of `download_model` is now strictly past rule-of-three; tracked as TD-010 in CONCERNS.md. Slice 2 (PR #23) added 9 integration tests across `tests/models_{download,list,remove}.rs` plus `fabricate_installed_model` / `fabricate_all_installed_models` helpers in `tests/common/mod.rs`. The fabricators use `std::fs::File::set_len` to create zero-filled sparse files — a 280 MB reranker fixture takes ~no disk space, and the all-zero contents intentionally disagree with the registry-pinned SHA-256, which the `--verify` test relies on to flip `checksum_mismatched`. The pattern is reusable for any future test needing realistic-size on-disk artefacts without IO cost. The `tome models download` CLI binary path is intentionally NOT tested end-to-end in CI (real `MODEL_REGISTRY` URLs = 66 MB embedder + 280 MB reranker downloads) — same boundary as `tome plugin enable`. CLI-level coverage is restricted to the skip path (all-installed → all-`skipped` records), exit-2 usage error, exit-30 missing-model, and exit-54 non-TTY refusal. Test total 195 → 204 across 28 → 31 suites. No new dependencies. No new error variants.
- 2026-05-13: Closed Phase 5 / User Story 3 across PRs #19–#21. Slice 1 (PR #19) added `src/commands/plugin/disable.rs` as a thin wrapper over the already-shipped `plugin::lifecycle::disable` orchestrator (PR #11) — `PluginCommand::Disable(PluginDisableArgs { id, force })` variant, `--force` short-circuits the confirm prompt, non-TTY without `--force` exits 54 (`NotATerminal`) with the documented pointer message ("Re-run with `--force` to skip the prompt"). Banner skipped in JSON mode (precedent: `enable::run`). Cheap re-enable (FR-006) is already in `index::skills::enable_plugin_atomic` — when `stored_hash == hash` the embedder closure is NOT invoked and the row is updated with `UPDATE skills SET enabled = 1`; T114 was therefore a verification task. Slice 2 (PR #20) added 3 integration tests in `tests/plugin_disable.rs` (CLI binary; disable doesn't construct `FastembedEmbedder`), 2 tests in `tests/plugin_repeated.rs` consolidating FR-008 (enable-of-enabled via library API per handover #10; disable-of-disabled via CLI binary for real `Some(21)` process exit), and `cheap_reenable_after_disable_invokes_embedder_zero_times` in `plugin_enable.rs` using `StubEmbedder::call_count()`. Promoted `paths_for(&ToolEnv) -> Paths` to `tests/common/mod.rs` at the 4th caller — resolves the P4 triage item. Closeout in PR #21 (`/sdd:map incremental` refresh). Test total 189 → 195 across 28 suites. No new dependencies. No new error variants.
- 2026-05-13: Closed Phase 4 / User Story 2 across PRs #16–#18. Slice 1 (PR #16) added `src/commands/plugin/interactive.rs` (`run_interactive` + three-level `catalog_loop` → `plugin_loop` → `view_loop` with a `LoopExit` enum encoding Back/Quit unwind vs propagated error), wired bare `tome plugin` via `Command::Plugin(PluginArgs { command: Option<PluginCommand> })`, and reuses `presentation::prompt::{select, confirm, require_terminal}` for TTY-gated interaction. Enable delegates to `enable::run`; Disable inlines `lifecycle::disable` + confirm. Slice 2 (PR #17) added `tests/plugin_interactive.rs` driven by a `rexpect = "0.7"` (dev-dep, Unix-only) pty harness — pre-enable via library API + StubEmbedder, then drive the read/write CLI through the pty; the CLI enable path is intentionally not exercised in CI because it loads `FastembedEmbedder` (~345 MB ONNX). Non-TTY case asserts exit 54 (`NotATerminal`) plus the documented pointer message. Closeout in PR #18 (`/sdd:map incremental` refresh + retro extraction). Test total 187 → 189 across 26 suites. New `paths_for(&ToolEnv) -> Paths` helper duplicated across `plugin_list.rs`, `plugin_show.rs`, `plugin_interactive.rs` — promote to `tests/common/mod.rs` at the 4th caller (likely Phase 5 `plugin_disable.rs`).
- 2026-05-13: Closed Phase 3 / User Story 1 across PRs #11–#15. Slice 1a (`plugin::lifecycle::enable` / `disable` orchestrator + pinned `MODEL_REGISTRY` SHA-256s; reranker URL moved upstream from `BAAI/bge-reranker-base` to `onnx-community/bge-reranker-base-ONNX`), slice 1b (`tome plugin enable | list | show` CLI + T074 prompt UI), slice 2 (`tome query` with reranker + `--strict`), slice 3 (5 new integration-test files + `tests/fixtures/sample-plugin-catalog/` + `StubEmbedder::with_force_fail_after`), and the resolver-bug fix folded into PR #14 (`lifecycle::resolve_plugin_dir` is now manifest-first via `tome-catalog.toml`; falls back to flat join only when the manifest is absent/unparsable). Constitution v1.2.0 — binary size cap revised 10 MB → 50 MB after slice 1b measured 29.56 MB on Linux (research §Binary size budget's ~9.2 MB worst-case projection underestimated `ort`). Test total 156 → 187 across 25 suites. T088 manual SC-001 / SC-002 verification against real BGE models is the only outstanding US1 task and lives in `retro/P3.md` for a developer pass.
- 2026-05-12: Closed Phase 2 foundational — landed slices 1–7 across PRs #2–#10. T057 (model-download integration test with hand-rolled `TcpListener` HTTP fixture) is in slice 7 rather than slice 5 where it was originally scheduled. The cleanup bug it caught (partial-dir leaking on checksum mismatch because cleanup only ran on `stream_to_partial` errors, not later pipeline errors) is fixed by wrapping the full post-stream pipeline in a closure. Codebase docs (`.sdd/codebase/STACK.md`, `STRUCTURE.md`) refreshed; retro at `specs/002-phase-2-plugins-index/retro/P2.md` extended with workarounds, package gotchas, patterns, and "for next time" entries.
- 2026-05-12: Generated Phase 2 `/sdd:plan` artefacts on `002-phase-2-plugins-index` — plan.md, research.md (15 R-decisions including binary-size strategy, SQLite concurrency model, schema migration, frontmatter strictness boundary), data-model.md, contracts/* (plugin-commands, query, models-commands, reindex, status, catalog-extensions, version-output, exit-codes, index-schema.sql), quickstart.md. Constitution gates: PASS with one justified deviation (`#[cfg(test)]` stub for the embedder/reranker traits — keeps CI fast and bounded; principle VIII boundary case).
- 2026-05-11: Generated Phase 2 `/sdd:specify` artefacts — spec (7 user stories, 60 FRs, 5 NFRs, 15 SCs) and refreshed `.sdd/codebase/*` against the Phase 1 source. Rust-lens review folded in 3 blockers + 12 majors before validation passed.
- 2026-05-11: Ratified CONSTITUTION.md v1.0.0; later patched to v1.0.1.
- 2026-05-11: Wrote Phase 1 PRD amendments resolving the constitution-review report.
- 2026-05-11: Generated `/sdd:specify` and `/sdd:plan` artefacts on `001-phase-1-foundations`. Constitution gates: PASS, zero violations.
- 2026-05-11: Added exit code 8 (SIGINT interrupted) after Rust-lens review of the Phase 1 spec.

<!-- MANUAL ADDITIONS START -->
<!-- Notes that should not be touched by automation go here. -->
<!-- MANUAL ADDITIONS END -->
